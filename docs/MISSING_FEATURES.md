# Inventory Management System — Missing Features

This document records the functionality that is missing or only partially implemented in the current application. It is intended to serve as a product backlog and implementation reference.

## Current baseline

The application currently provides a basic Laravel POS workflow covering:

- User authentication, registration, email verification, password reset, profile, and password change.
- Supplier, customer, unit, category, and basic product masters.
- Purchase entry, pending purchase approval, and quantity increase after approval.
- Sales invoice entry, pending invoice approval, stock validation, and quantity decrease after approval.
- Full, partial, and due customer payments.
- Basic stock, purchase, invoice, supplier-wise, product-wise, and customer payment reports.
- Invoice and report print/PDF views.

The legacy system still stores a compatibility quantity on each product, while the ERP foundation now maintains transaction-based inventory movements, warehouse locations, accounting journals, and enterprise authorization alongside it. Several advanced workflows remain partial and are tracked below.

### Implemented in the initial ERP foundation upgrade

The first foundation slice has now been added to the codebase:

- Company, branch, department, warehouse, store, and user organization fields.
- Roles, permissions, role-user assignments, and permission-role assignments.
- Warehouse location hierarchy for warehouse, zone, rack, shelf, and bin records.
- Immutable inventory movement records and a reusable inventory ledger service.
- Audit-log storage and approval audit events.
- Approved purchase receipt and sales issue movements, while retaining the legacy product balance for compatibility.
- Initial system administrator role and core permission seed data.
- Product master extension with brand, SKU, barcode, HSN/SAC, pricing, tax, stock thresholds, and batch/serial tracking mode.
- Stock movement ledger report with product, movement-type, and date filters.
- Controlled stock adjustment workflow with reason codes, approval/rejection, quantity locking, audit events, and ledger postings.
- Location-based inventory transfer workflow with source/destination validation and paired ledger movements.
- Organization and location setup screens for companies, branches, warehouses, and storage locations.
- Purchase-order and goods-receipt workflow with partial-receipt quantities and stock posting on receipt approval.
- Sales-order and delivery workflow with partial-delivery quantities and stock issue on delivery approval.
- Customer sales returns and supplier purchase returns with approval, stock impact, audit, and ledger movements.
- Batch/lot/serial receipt metadata with manufacturing, expiry, best-before, warranty fields, and serial-count validation.
- FIFO cost layers, cost consumption records, and stock valuation report for new ledger movements.
- Finance configuration tables for currencies, exchange rates, tax rates, fiscal years, and numbering sequences.
- Chart of accounts, journal entries, journal lines, balanced debit/credit validation, and accounting posting service.
- Chart-of-accounts administration and journal-entry visibility screens.
- Configurable automatic account mappings for inventory receipt, COGS, loss, and related journal postings.
- Planning exception report for low, excess, slow-moving, and dead stock.
- Purchase suggestion report based on reorder level and target stock.
- Sales-order stock reservations with automatic release as deliveries are approved.
- Sales invoice subtotal, tax, grand-total fields and product-tax calculation service.
- A legacy backfill command: `php artisan erp:backfill-inventory-ledger`.

These tables and services form a backward-compatible ERP foundation; advanced workflow depth, external integrations, and complete enterprise controls remain tracked below.

## Current implementation estimate

This is a scope estimate, not a formal release gate. Against the original 14-category checklist, approximately **80–85% of the functional surface has at least a working implementation**, while approximately **15–20% remains** for full universal-ERP depth. The 192 rows below are intentionally still marked `Partial`: they describe the remaining enterprise-depth work as well as capabilities that already have a usable baseline. The current foundation estimate should not be reported as 99% completion of the entire ERP product.

Verification baseline: `vendor/bin/phpunit tests/Feature` passes with **74 tests and 508 assertions**; the HR/payroll increment specifically passes **1 test and 57 assertions**.

## Status definitions

- **Missing** — no functional implementation exists.
- **Partial** — a basic related function exists, but the requested capability is incomplete.
- **Target** — the expected capability after implementation.

## 1. Organization and setup

| Feature | Status | Target |
|---|---|---|
| Company | Partial | Company creation and core details are available; authorized browser administrators and company-scoped integration clients can update and deactivate their authorized company profile with cursor synchronization, and both browser/API paths prevent deactivation while active branches or departments remain; logo, defaults, and complete business settings remain. |
| Branch | Partial | Branch creation and company linkage are available with current-company ownership validation; browser and integration clients can synchronize, update, deactivate, and create authorized branches with idempotent external references, and both branch deactivation paths protect active warehouses and stores; users assigned to a branch now receive branch-scoped warehouse/store/location reads and model-enforced writes through the shared branch-company boundary, while full settings and user scope remain. |
| Warehouse | Partial | Warehouse creation and branch linkage are available; browser and integration clients can synchronize, update, deactivate, and create authorized warehouses and locations with hierarchy filters, quantity/weight/volume capacity controls, tenant-scoped location rules, idempotent external references, and model-enforced assigned-branch write isolation; both warehouse deactivation paths protect active child locations, while advanced operational controls remain. |
| Store | Partial | Stores can be created and browser-maintained with branch linkage, code, address, active state, duplicate-code protection, active fulfillment-warehouse assignment, currency, tax mode, and negative-stock/POS defaults, and can be linked to sales orders and invoices; integration clients can synchronize, update, deactivate, and create authorized stores with assigned-branch write isolation, and both store deactivation paths protect active sales orders, while store-specific users/richer POS settings remain. |
| Departments | Partial | Departments can be created and browser-maintained with company linkage, code, active state, and duplicate-code protection; integration clients can synchronize, update, deactivate, and create company-scoped departments, and both department deactivation paths protect active assigned users, while cost-center/operational ownership remain. |
| Users and roles | Partial | Role/permission administration, activation/deactivation, and company/branch/department scope are available; role assignment and user administration are constrained to the current company for tenant users, assigned branch users now receive branch-scoped warehouse/store/location reads, while credential-free, company-scoped user/role/permission synchronization feeds are available and broader policy enforcement remains. |
| Fiscal years | Partial | Fiscal-year records are company-scoped, overlapping periods are rejected, a close checklist blocks closure while approval-pending operational/accounting documents exist or the captured immutable company/date inventory snapshot contains negative-balance variance, closure persists the balanced snapshot reference, periods can be explicitly closed with audit history and reopened only with a mandatory reason, and accounting plus central inventory postings reject dates outside an open period; accounting integrations can read/create periods and perform guarded close/reopen actions, while broader period-end reconciliation and settlement controls remain. |
| Numbering sequences | Partial | Configurable atomic sequences now support purchase orders, goods receipts, sales orders, deliveries, production orders, maintenance orders, adjustments, returns, stock counts, status transfers, supplier invoices, payments, landed costs, and legacy sales invoices; operational document-number uniqueness is now company-scoped, while payment/legacy fallback cleanup remains. |
| Tax configuration | Partial | Company-scoped tax-rate administration, effective-dated rate versions with overlap protection, customer/supplier exemption certificates, party tax-jurisdiction preferences, jurisdiction-aware same-code tax selection, and persisted inclusive/exclusive invoice calculation are available; accounting integrations can read, create, and update tenant-scoped tax rates with audit history, while jurisdictional filing and rules remain. |
| Currencies | Partial | Currency administration and precision are available; sales/purchase orders, invoices, and customer/supplier payments retain transaction currency and conversion rate while journals post converted base values; accounting integrations can read/create/update currencies with uppercase normalization and serialized base selection; open AR/AP foreign-currency revaluation reporting, controlled idempotent gain/loss journal posting, and cross-currency AR/AP allocation snapshots are available, while settlement remains. |
| Exchange rates | Partial | Effective-dated direct/inverse rate lookup now populates invoice and payment conversion rates and supports an explicitly company-scoped as-of open-balance revaluation report plus controlled gain/loss journal posting; accounting integrations can synchronize rates and publish active-currency rates with audit history, while rate source governance and settlement remain. |
| System settings | Partial | Company-scoped settings now centralize date format, decimal precision, default tax mode, negative-stock policy, document print size, purchase variance, discount limits, expiry policies, planning calendar, and ABC thresholds with an administration screen; authenticated accounting integration clients can read/update the supported settings with tenant validation and audit history, while broader defaults and per-branch overrides remain. |

## 2. Product master

| Feature | Status | Target |
|---|---|---|
| Product/item | Partial | Add product master fields, tracking mode, ownership, active status, product type, lifecycle status, and purchase/sale eligibility flags; tenant-safe idempotent integration APIs and browser workflows now create, update, and deactivate products with company-validated supplier/unit/category/brand references, ledger-first stock/tracking safeguards including held balances, and protected archive/non-stock transitions, and purchasing/sales creation plus approval workflows enforce lifecycle eligibility; bundle products now have a tenant-safe component master with quantity, ordering, circular-reference protection, audit, component-aware sales-order reservation, component ledger issue during delivery, allocated bundle pricing, guarded cancellation routing to sales returns, and component-specific serial capture during return expansion, while richer catalog governance and omnichannel bundle presentation remain. |
| SKU | Partial | Company-scoped unique SKU field and validation are available; admin-created products and integration-created products/variants can auto-generate collision-checked SKUs from the configurable `product` numbering sequence (or use `auto_sku=false` for integrations), while search, import/export, and broader numbering governance remain. |
| Barcode/QR code | Partial | Company-scoped unique primary barcode plus normalized multiple barcode/QR-code records, primary-code compatibility mirroring, authenticated POS lookup, tenant-safe integration lookup by barcode/QR/SKU with optional location availability, idempotent external-reference barcode creation, printable Code 39 labels, QR PNG label generation through configurable `qrencode`, and tenant-safe integration barcode creation/feed exposure are available; scanner hardware integration and hosted encoder fallback remain. |
| Product categories | Partial | Company-scoped category administration now supports hierarchy, company-scoped codes, active state, cycle-safe parent changes, tax defaults inherited by new products, guarded existing transaction deletion, browser/API-managed required variant attributes enforced during browser/API variant creation; category-level reporting remains. |
| Brands | Partial | Company-scoped brand administration now supports create, update, company-scoped codes, active state, product counts, audit, and guarded deletion; hierarchy and richer brand media remain. |
| Units of measure | Partial | Company-scoped UOM records now support normalized, database-unique codes, decimal precision, dimensional classification, and base-unit metadata; reusable company-scoped conversion rules with same-dimension validation, effective date ranges, idempotent API synchronization, lifecycle controls, and product-specific override fallback are available, while rate-history governance remains. |
| Multiple UOMs | Partial | Product-specific conversions normalize purchase/sales orders, goods receipts, and deliveries into stock units while retaining entered UOM quantities, enforcing purchase/sales usage, and rejecting configured cross-dimension conversions; reusable company-scoped conversion rules provide a validated fallback with product-specific overrides, while browser administration and authenticated inventory APIs expose tenant-safe UOM conversion read/create/update/deactivate operations with precision and usage controls, and manufacturing/reporting UOM fields remain. |
| Product variants | Partial | Parent products, variant SKUs, inherited master data, lifecycle eligibility, purchase/sales flags, and attribute assignments are available; browser and tenant-safe integration APIs create variants only from active parents with inherited product controls, active tenant-validated attributes, and one-value-per-attribute validation, while variant-specific pricing, media, and lifecycle overrides remain. |
| Attributes | Partial | Company-scoped reusable attributes and values can be managed and assigned to variants with tenant-scoped name/value uniqueness and tenant-validated references; integration APIs expose cursor-based attribute feeds and create attributes/values with duplicate protection, while attribute sets, validation rules, and reporting remain. |
| Product images/documents | Partial | Private product attachments now support image/document type, title, version, primary-media selection restricted to images, MIME validation, automatic per-record media/document version increments, inline upload/list/download controls, authenticated image previews on product edit screens, primary-image thumbnails on the product list, and tenant-safe media metadata synchronization through the inventory API; galleries remain. |
| HSN/SAC | Partial | Product classification field is available; jurisdictional validation and reporting remain. |
| Tax rates | Partial | Products can reference company-scoped effective-dated tax-rate records while retaining the legacy numeric rate; customer/supplier jurisdictions can select a matching same-code effective rate and the chosen party jurisdiction is snapshotted on sales/purchase invoices, with inclusive/exclusive calculation and certificate-backed exemptions persisted; jurisdictional filing rules remain. |
| Purchase pricing | Partial | A default purchase price, supplier-specific agreements, and reusable company-scoped purchase price lists with currencies, validity dates, and quantity breaks are available; integrations can create/deactivate purchase lists and add repeat-safe quantity breaks, while richer price selection and import/version controls remain. |
| Sales pricing | Partial | Default sales prices plus deterministic customer-specific, customer-group+channel, group, and channel quantity-break agreements and reusable assigned company-scoped sales price lists with validity, matching currency, and discounts are available; integrations can create/deactivate sales lists and add repeat-safe quantity breaks, while richer named-list selection remains. |
| Minimum/maximum stock | Partial | Product-level thresholds and company-scoped location policies are available; the browser and integration planning reports apply location min/max values when a location is selected, while broader policy versioning remains. |
| Reorder level | Partial | Product-level reorder level and company-scoped location reorder points are available; browser/API planning and replenishment use the selected location policy with lead time, safety stock, forecast, and replenishment automation, while broader policy versioning remains. |
| Product import/export | Partial | Validated CSV and native XLSX import/export now share header, required-field, foreign-key, numeric-range, tracking-type, product-type, lifecycle-status, eligibility-flag, status, duplicate SKU/barcode checks, dry-run preview, and downloadable validation errors; richer templates and scheduled bulk jobs remain. |

## 3. Inventory operations

| Feature | Status | Target |
|---|---|---|
| Opening stock | Partial | Dedicated opening-stock entry and `erp:import-opening-stock` CSV command capture approved, location-aware quantities, unit costs, batch/lot dates, and serial numbers and post traceable `opening` ledger movements with audit history; richer migration mapping remains. |
| Stock receipt | Partial | Independent receipt documents now support product lines, location, cost, batch/lot and serial capture, optional pass/fail quality inspection with reviewer audit, approval, ledger posting, serial-linked movement allocations, and accounting integration outside purchase orders; discrepancy handling remains. |
| Stock issue | Partial | Independent issue documents now support controlled product lines, location, document- or line-level department and cost-center allocation, reason, selected batch/serial issuance, maker/checker approval, central negative-stock prevention, audit, ledger, and accounting posting; full multi-batch entry remains. |
| Stock transfer | Partial | Transfers now progress through approved, dispatched/in-transit, and received states with company/branch-validated locations, reasoned pre-dispatch rejection, source validation, paired ledger movements, operation actors, receiving variance disposition, carrier/tracking/ETA fields, transfer-specific inventory-to-transit/to-inventory accounting, and accepted/waived shortage settlement from inventory-in-transit to inventory-loss when mappings exist; route integration and broader reconciliation remain. |
| Stock adjustment | Partial | Positive/negative adjustment workflow, reasoned rejection, approval, audit, batch-aware outbound validation, ledger posting, costing, and configured inventory/inventory-loss journals are available; variance reconciliation and richer disposition controls remain. |
| Stock reservation | Partial | Sales-order and production-component reservations support optional order locations, explicit owning-company availability checks across browser/API/queued fulfillment paths, tenant-scoped location-aware availability derived from the approved ledger with legacy fallback only where no ledger history exists, grouped availability queries for planning-scale requests, FEFO batch/lot assignment when cost layers exist, release on fulfillment, permission-protected reassignment/release, and scheduled backorder allocation; the reservation integration feed exposes assigned batches and supports status/product/location filters and signed cursor synchronization, while service reservations and allocation optimization remain. |
| Stock return | Partial | Sales and purchase return workflows support optional company-validated warehouse location and batch selection, reason, approval, audit, batch/serial-aware ledger movements, consistent web/API source-document type validation, row-locked source-document quantity validation including per-batch limits, optional approval-gated quality inspection, and sales returns can now reverse delivered quantities against an approved delivery with customer/source matching; bundle sales returns expand to leaf components and accept component-specific serial assignments, while quarantine disposition remains. |
| Damaged stock | Partial | Approved status transfers and separate damaged balances are available; inspection workflow and recovery valuation remain. |
| Scrap | Partial | Approved scrapping decreases stock, posts a scrap movement, and records audit history; recovery value and detailed reporting remain. |
| Quarantine stock | Partial | Approved or rejected quarantine/damaged status transfers and balances are available in the web UI and authenticated integration API, including optional independently inspected release back to available stock with inspector, timestamp, notes, and audit history; richer release workflow remains. |
| Cycle counting | Partial | Count documents capture company-validated products and locations with variances, approval, reasoned rejection, snapshot selected locations from the centralized company/location physical balance service, and freeze matching ledger movements while submitted; recurring schedules create safe draft count documents and daily due-document generation is available, and approval can require an independently entered, audited second count before posting; richer multi-counter assignment and recount variance policies remain. |
| Physical verification | Partial | Submitted counts reconcile approved variances into the legacy balance and location ledger, with centralized freeze protection against concurrent movements; recount and full multi-location reconciliation remain. |
| Inventory ledger | Partial | Application-level immutable ledger rows now reject updates/deletes and require correcting movements, centrally reject non-stock products, use explicit source-company availability and stock-count freeze checks, and support explicit company-scoped bulk availability for queued/integration planning and reporting paths; the read-only `erp:reconcile-inventory-ledger` command can now perform an explicitly requested, reasoned synchronization of legacy balances from ledger history with audit events, while automatic correcting movements and broader period reconciliation remain. |
| Stock status | Partial | Available, reserved, blocked/hold, quarantine, damaged, and scrap balances/transfers are tracked, status balances/transfers are available through authenticated cursor-based APIs, and transfers between unavailable statuses no longer create false physical ledger availability; location-specific allocation and full disposition rules remain. |
| Negative stock control | Partial | Central ledger validation now prevents outbound movements from exceeding available physical stock at the selected location; company policy supports block, unrestricted allow, and approval-only override, where an approved document must carry an independent checker before it may post negative stock, and a company-authorized store-level allow flag applies to store-linked postings, while richer exception reporting remains. |

## 4. Warehouse management

| Feature | Status | Target |
|---|---|---|
| Warehouse master | Partial | Warehouse creation and browser/integration administration include branch, manager, address, active status, warehouse type, location capacity, and typed warehouse-feed filtering; deactivation protects active child locations, while advanced operational controls remain. |
| Zone | Partial | Generic warehouse locations support zone records and hierarchy; browser and integration administration can manage tenant-scoped product/category allow/deny rules that inherit through the location tree and category hierarchy and are enforced on inbound posting, while utilization remains. |
| Rack | Partial | Generic warehouse locations support rack records and hierarchy; browser administration now edits/deactivates rack metadata with tenant-safe parent and capacity validation; rack utilization remains. |
| Shelf | Partial | Generic warehouse locations support shelf records and hierarchy; browser administration now edits/deactivates shelf metadata with tenant-safe parent and capacity validation; shelf utilization remains. |
| Bin | Partial | Generic warehouse locations support bin codes, capacity, hierarchy, tenant-scoped product/category restrictions, and browser edit/deactivation controls; centralized inbound ledger posting now prevents capacity or zoning overflow, including inherited parent-category rules, while barcode restrictions remain. |
| Put-away | Partial | Capacity- and zoning-aware browser and integration planners rank active bin/shelf/rack locations using ledger occupancy and can create approval-required internal transfer tasks from a receiving location to the selected destination; both paths enforce quantity/weight/volume checks with tenant/same-warehouse validation, while receipt confirmation and barcode restrictions remain. |
| Picking | Partial | Delivery pick operations now persist confirmed quantities per delivery line, enforce exact quantity confirmation before packing, retain performer audit, and are required before dispatch approval; pick-list optimization and scanner workflows remain. |
| Packing | Partial | Delivery pack operation is enforced after picking, package dimensions/weight can be recorded, packed quantities are confirmed per delivery line, and packing is required before dispatch approval; package records, contents optimization, and carrier integrations remain. |
| Dispatch | Partial | Approved delivery is marked as dispatched with an operation record only after completed picking and packing; carrier workflow and proof of delivery remain. |
| Receiving | Partial | Goods receipts support PO matching, partial quantities, batch/serial capture, optional pass/fail quality inspection with reviewer audit, approval, location posting, and centralized capacity enforcement; discrepancy handling and transfer receiving remain. |
| Warehouse-to-warehouse transfer | Partial | Location transfers support request, approval, dispatch, in-transit tracking, destination receipt, partial quantities, immutable batch/serial allocations with per-allocation receipt quantities, explicit reasoned shortage closure, receiving notes, carrier/tracking/expected-arrival capture, paired serial-linked or aggregate ledger movements, transfer-specific inventory-to-transit/to-inventory accounting, source validation, serial allocation, audit actors, and approval-protected accepted/waived variance disposition with reason, resolver audit, and inventory-loss/in-transit settlement when mappings exist; ability-scoped APIs expose transfer allocations and variance resolution, while broader reconciliation remains. |
| Location-level stock | Partial | Ledger-derived balances can be reported by warehouse and storage location, and receipts/deliveries/issues/returns validate and post against the selected location; location availability now avoids double-counting quarantine/damaged balances because status movements are already represented in the ledger; authenticated `GET /api/inventory/warehouse/location-utilization` now exposes tenant-safe per-location quantity, weight, volume occupancy, capacity, utilization percentages, and configurable `normal`/`warning`/`full`/`not_configured` capacity status with warehouse/type filters; transfer allocation and reconciliation remain. |

## 5. Batch, lot, serial, and traceability

| Feature | Status | Target |
|---|---|---|
| Batch number | Partial | Batch number capture and batch record creation on goods-receipt approval and production completion are available; explicit issue batch selection flows into cost-layer consumption and immutable movement-level allocations, and mixed-batch FIFO/FEFO issues expose every consumed batch in traceability feeds, while full batch reservation/allocation remains. |
| Lot number | Partial | Lot identity is stored on batch records, shown in expiry reporting, and retained through immutable movement-level allocations for mixed-layer issues; full lot reservation/allocation remains. |
| Serial number | Partial | Serial capture and uniqueness validation on receipt and production completion are available; independent stock issues and serialized sales/purchase returns now write one ledger movement per serial, return transitions require issued serials, sales/issues select available serials at the posting location with legacy global fallback and mark them issued, and warehouse transfers allocate exact serials through destination receipt, while service lifecycle tracking remains. |
| Manufacturing date | Partial | Stored on receipt batch records; reporting remains. |
| Expiry date | Partial | Stored on receipt batch records; central ledger posting now blocks expired-batch outbound movements by default with a company-configurable override, while alerts and jurisdiction-specific controls remain. |
| Best-before date | Partial | Batch field exists, participates in FEFO ordering when no expiry date is present, and is retained in movement allocations and expiry reporting; company administrators can explicitly allow or block outbound issue past best-before, while automated warning policy remains. |
| Warranty | Partial | Warranty-until metadata is supported for received batches/serials; claims and service history remain. |
| Batch-wise stock | Partial | Ledger-derived batch/location balances now include immutable movement allocations for mixed-batch issues, with batch expiry context and tenant-safe ownership; authenticated `GET /api/inventory/batches/stock` exposes product, batch, location, updated-since, zero-balance, quantity, and value filters, while full transaction controls remain. |
| Serial-wise stock | Partial | Serial records retain a posting/holding location and lifecycle status can be filtered and reported with direct product-owner tenant scoping; authenticated `GET /api/inventory/serials` now exposes tenant-safe product, batch, location, status, and deterministic cursor/updated-since filters for integration consumers, while transfer and service handoff transitions remain. |
| FEFO | Partial | Batch/lot-tracked costing layers are consumed in earliest expiry-or-best-before order when no batch is explicitly selected, and every selected layer is persisted on the movement allocation record; expiry blocking and full reservation controls remain. |
| FIFO | Partial | FIFO cost layers allocate oldest receipts for untracked items, with every consumed layer persisted as an immutable movement allocation; complete source traceability remains. |
| Traceability | Partial | Searchable movement history and integration feeds link products to batch/lot, serial, location, actor, and source references, including every batch/lot allocation for mixed-layer issues; browser traceability filters now validate batch/serial ownership through the tenant product relationship, while supplier/customer chain-of-custody and service handoff remain. |
| Expiry alerts | Partial | Configurable batch expiry reports, positive-stock filtering, repeat-safe database notifications, scheduled `erp:inventory:expiry-alerts` delivery, and an in-app notification inbox are available; alerts now include value-at-risk from remaining cost layers with purchase-price fallback, while override approvals and full issue batch linkage remain. |

## 6. Purchasing

| Feature | Status | Target |
|---|---|---|
| Purchase requisition | Partial | Create, submit, approve, reasonedly reject, audit, and convert internal purchase requests into prefilled purchase orders; multi-stage routing and line-level fulfillment remain. |
| RFQ | Partial | Issue multi-supplier RFQs with requested lines, response due dates, supplier responses, reasoned rejection, and close workflow; email delivery and requisition-linked sourcing remain. |
| Purchase quotation | Partial | Record, compare, and award complete supplier quotations into submitted purchase orders; requisition linkage, commercial approval, and supplier portal integration remain. |
| Purchase order | Partial | Create, submit, approve, reject, track, and auditable-cancel company-scoped purchase orders; supplier, product, UOM, and receipt locations are tenant-validated, and receiving can be explicitly closed after a complete final delivery or an accepted final-delivery shortage, while revision, closure reporting, and advanced matching remain. |
| Goods receipt | Partial | Separate company-scoped receipt documents, partial quantities, optional final-delivery declaration, recorded shortage reason, approval, reasoned pre-posting rejection through browser and integration API, batch/serial receiving, selected-location stock posting, duplicate-serial prevention, and serial-linked ledger movements are available; final receipts with outstanding PO quantities open an auditable discrepancy that a different authorized checker can resolve with an explicit shortage-acceptance reason through the integration API, and the procurement feed exposes final-delivery/discrepancy state with filters, while discrepancy settlement and supplier claims remain. |
| Purchase invoice | Partial | Supplier invoices include jurisdiction-aware tax resolution from supplier preferences, tax-jurisdiction snapshots, persisted due dates from explicit input or supplier payment terms, approval, reasoned rejection before posting, and PO/receipt quantity matching; payment lifecycle and richer variance handling remain. |
| Purchase return | Partial | Supplier returns support optional validated posting location and batch selection, reason, approval, row-locked source goods-receipt matching with supplier/product/batch/quantity validation, stock reduction, batch/serial-aware ledger movement, and source-aware AP/GRNI reversal accounting; supplier claims can now be created against a goods receipt or purchase return with idempotent references, auditable submitted/accepted/rejected/settled transitions, and a tenant-safe integration feed, while supplier credit notes and settlement remain. |
| Supplier master | Partial | Supplier master now stores tax identity, payment terms, bank details, rating, and active state alongside structured billing/shipping/contact records with one-default-per-type handling; tenant-safe, retry-safe integration APIs can create/update suppliers and contacts and expose deterministic filtered synchronization feeds with tax and payment validation, while contact history, compliance documents, and supplier lifecycle workflows remain. |
| Supplier pricing | Partial | Maintain company-scoped supplier-item prices with quantity breaks, currency, effective dates, supplier SKU, and lead time; purchase orders and MRP now use one tenant-scoped, currency-aware, date/quantity-valid price-selection service, and agreements can be deactivated with audit history, while import/version controls remain. |
| Supplier performance | Partial | Report supplier order activity, received quantities, fill rate, on-time receipt rate, failed/inspected receipts, rejected quantities, quality pass/rejection rates, transparent weighted scorecards (fulfillment 40%, on-time 30%, quality 20%, price 10%), price variance, configurable periods, and a tenant-safe procurement integration feed; corrective-action workflows remain. |
| Three-way matching | Partial | Invoice approval checks purchase-order and approved-receipt quantities, uses the linked approved receipt's actual unit cost as the price-variance baseline (with purchase-order fallback for legacy/unlinked lines), rejects mismatched or unapproved receipt references, enforces configurable company-level purchase-price variance tolerance, and can post AP/GRNI journals; a supplier/invoice variance report is available, while variance workflows and payment controls remain. |
| Purchase approvals | Partial | Configurable amount-based policies now support ordered multi-step permissions with persisted approver actions, and purchase invoice approval enforces optional price-variance tolerance; category/branch routing, date-bounded delegation, rejection, and escalation remain. |

## 7. Sales

| Feature | Status | Target |
|---|---|---|
| Sales quotation | Partial | Company-scoped quotation creation, expiry validation, approval, reasoned rejection, audit, and conversion into company-scoped sales orders are available; revisions, version history, and richer pricing remain. |
| Sales order | Partial | Company-scoped sales orders support optional store/location context, promotion-code discounts prorated across eligible lines, approval, reasoned rejection, strict reservations, explicit backorder approval, partial delivery, reservation release, and scheduled deterministic allocation of newly available stock; backorder prioritization rules and advanced pricing remain. |
| Picking | Partial | Delivery pick operations persist confirmed quantities per delivery line and are enforced before packing; ability-scoped integration clients can complete picking with exact line quantities, while pick-list optimization and scanner workflows remain. |
| Packing | Partial | Delivery packing is tracked, package dimensions/weight can be recorded, and packed quantities are confirmed per delivery line after picking; ability-scoped integration clients can complete packing with exact line quantities, while package records, contents optimization, and carrier integrations remain. |
| Delivery | Partial | Company-scoped delivery documents, address, carrier, tracking, partial quantities, reasoned pre-posting rejection, approval, batch/serial capture, serial-linked stock issue, proof of delivery, dispatch, and sales-order fulfillment-location consistency are available; partial/cancelled states and carrier/POD integration remain. |
| Sales invoice | Partial | Sales invoices now snapshot billing/shipping addresses from explicit integration input, validated customer billing/shipping contacts, or the customer master, resolve tax using the customer jurisdiction when configured, snapshot that jurisdiction, apply eligible promotion codes, capture optional batch/serial line data and customer credit-term or explicitly entered due dates, validate serialized quantities at approval, support reasoned rejection before posting, and post serial-linked ledger movements alongside tax, payment, currency, and accounting context; tenant-scoped integration APIs support idempotent create plus approval/rejection; full allocation remains. |
| Sales return | Partial | Sales-return approval, optional validated posting location and batch selection, optional row-locked source-invoice matching with customer/product/batch/quantity validation, certificate-backed exemption snapshots, approval-gated quality inspection, batch/serial-aware stock posting and serial restoration, bundle-to-component return expansion with component-specific serial propagation, sales value/tax capture, and controlled customer refund submission, approval, or reasoned rejection with journal posting only on approval are available; customer-refund creation now supports company-scoped idempotent external references, while quarantine disposition and refund settlement integrations remain. |
| Customer master | Partial | Customer credit controls are now configurable in the customer UI, alongside structured billing/shipping contacts with relation-based tenant isolation and one-default-per-type handling; tenant-safe, retry-safe integration APIs can create/update customers and contacts with external references and expose deterministic filtered customer/contact synchronization feeds with credit and tax validation, while richer customer lifecycle and compliance workflows remain. |
| Customer pricing | Partial | Maintain company-scoped customer-item, customer-group, and sales-channel prices with quantity breaks, currency, validity windows, discounts, customer-first automatic order-line fallback, and audited non-destructive deactivation; richer named price lists remain. |
| Discounts | Partial | Manual discounts plus promotion eligibility by item/category/customer, minimum quantity, date, percentage, and fixed value are available; ERP sales orders and sales invoices now persist applicable discounts and a company-scoped maximum-discount setting blocks approval above the threshold unless the approver has `sales.discount.override`, with browser/API enforcement; richer discount bands and exception workflow remain. |
| Promotions | Partial | Company-scoped promotion master and coupon codes, validity, usage limits with atomic redemption at invoice or sales-order approval, audit, and document traceability are available; percentage/fixed discounts and same-product buy-one-get-one quantity rules are supported through browser/API creation and sales order/invoice pricing flows, and API sales documents support explicitly stackable promotion codes with persisted applied IDs and atomic redemption, while bundles, campaigns, and omnichannel rules remain. |
| Credit control | Partial | Customer credit limits, payment terms, manual holds, overdue balance/days assessment, collection status, and configurable overdue-day blocking during browser and integration sales-order approval are available; reminders and settlement controls remain. |
| Delivery status | Partial | Delivery lifecycle now tracks pending, dispatched, delivered, and auditable cancelled states; pending cancellation and dispatched-but-not-delivered cancellation with batch-aware stock/serial reversal, dispatch operation, delivery timestamp, and cancellation reason/actor are recorded, the integration feed can filter deliveries by the parent sales order’s approved/partially-delivered lifecycle, delivery confirmation accepts an auditable POD reference and controlled timestamp, immutable carrier tracking events support provider identity/raw-payload retention, idempotency, status/location/time synchronization, and forward-only dispatch/delivery lifecycle synchronization only after a valid dispatch operation, and approved-delivery returns provide the controlled post-delivery reversal path; a generic adapter contract/registry is available, while concrete carrier connectors and richer partial-state presentation remain. |

## 8. Manufacturing

| Feature | Status | Target |
|---|---|---|
| Bill of Materials | Partial | Define finished goods, components, quantities, scrap factors, effective dates, explicit version metadata, and company-scoped manufacturing masters; referenced BOM revisions are immutable, active revision date ranges cannot overlap, and new versions can be created, while revision approval and immutable nested snapshots remain. |
| Multi-level BOM | Partial | Recursive active-BOM explosion supports subassemblies and circularity detection, validates every root finished good, component, by-product, and nested finished-good revision against the planning company, resolves effective child revisions using the production order planned date, and production orders retain an immutable effective BOM tree snapshot plus selected version; revision approval and richer revision comparison remain. |
| Routing | Partial | BOM-linked routings now define ordered operations with setup/run times and work-center assignment; production orders initialize and execute those operations in sequence, and finished-goods receipt requires all routing operations to be completed or skipped, while finite scheduling remains. |
| Work center | Partial | Company-scoped work-center codes now store capacity, labor/machine rates, calendars, and active state; authenticated manufacturing integration now reports completed-operation utilization plus planned capacity-load slots, setup/run hours, quantity, utilization, and overload flags, while shifts and availability planning remain. |
| Production order | Partial | Create, release, consume components, receive finished goods in one or more controlled partial receipts, and cancel open orders with reservation release at an optional production location with sequence-backed order numbers; creation requires an active BOM effective on the planned date and records its version for traceability, while pause/close controls remain. |
| Material issue | Partial | Production release reserves components and completion consumes them through the inventory ledger, including reservation-aware batch selection and serial lifecycle/ledger issuance for serial-tracked components; separate issue documents remain. |
| Material consumption | Partial | BOM-based component consumption and proportional reservation release now validate active stock-managed components, use explicit production-company availability checks at the production order location, preserve batch reservations, and issue serial-tracked components through serial lifecycle and per-serial ledger allocations for each production receipt; substitutions and variance capture remain. |
| Finished goods | Partial | Production receipts require active stock-managed finished goods and by-products plus completed routing operations, receive them into the selected location and ledger, retain cumulative completed quantity with serial-safe full completion, include work-center labor/machine operation costs using actual times with routing-time fallback, and persist material/operation/by-product/net cost breakdowns across partial receipts; production lot/serial allocation and full cost variance capture remain. |
| By-products | Partial | BOMs can define by-products with output quantities and cost-share percentages; production completion receipts and values them, while yield variance and co-product settlement remain. |
| Scrap | Partial | Production scrap factors and inventory scrap movements are supported; dedicated production scrap capture remains. |
| WIP | Partial | Production orders now expose operation-level pending/in-progress/completed states, work-center assignment, quantities, actual setup/run times, audit history, operation-cost contribution to finished-goods valuation, and cumulative cost breakdowns; an authenticated WIP feed reports open orders, completion percentage, remaining quantity, and accumulated production cost, while operation-level WIP valuation remains. |
| Production planning | Partial | BOM-aware production suggestions include component availability and can prefill a production order; scheduled erp:planning:generate-production-orders creates idempotent draft orders for finished goods below target after netting open purchase and production quantities, and a tenant-scoped capacity-load feed compares planned operation hours with work-center daily capacity, while finite scheduling remains. |
| MRP | Partial | MRP net-requirement report aggregates leaf materials from active multi-level BOMs with explicit tenant-scoped availability, nets ledger availability together with open purchase-order and open production-order supply, exposes the supply breakdown, and supports prefilled purchase orders for buy items; lead times, safety stock, planned-order batching, and capacity remain. |
| Manufacturing integration API | Partial | Ability-scoped cursor feeds expose company-scoped production orders, BOMs, and independently changing shop-floor operations with statuses, planned/completed quantities, production locations, operations/work centers, components, by-products, versions, effective-date filters, and updated-since synchronization; production suggestions expose target, available, open-order, max-build, and shortage details, while grouped production-cost, work-center utilization, WIP, and planned capacity-load feeds are available; idempotent write-side BOM creation/update/deactivation and production-order creation plus approval-protected release, partial/full completion, cancel, and controlled operation start/complete actions are available through `manufacturing:write`, while provider adapters remain. |

## 9. Inventory valuation and costing

| Feature | Status | Target |
|---|---|---|
| FIFO costing | Partial | FIFO layers and outbound cost consumption are available for ledger movements, with immutable batch/lot allocations for every consumed layer; legacy backfill and full accounting integration remain. |
| Weighted average | Partial | Product-selectable weighted-average layer revaluation and consumption are available; periodic close/revaluation controls remain. |
| Standard cost | Partial | Product-selectable standard cost is used for issue valuation; revisions and production/purchase variance reporting remain. |
| Moving average | Partial | Product costing now supports moving-average selection and revalues open cost layers on each approved receipt; adjustment revaluation and periodic close controls remain. |
| Landed cost | Partial | Approved freight/duty/insurance costs can be allocated by receipt value or quantity into receipt cost layers, update recorded COGS for already-consumed quantities, retain immutable per-layer before/after cost adjustments, and lock the landed-cost lifecycle so concurrent/repeated approval cannot duplicate allocations; when `inventory`, `cogs`, and `landed_cost_clearing` mappings exist, a balanced inventory/COGS-to-clearing journal is posted. Controlled browser and procurement integration reversals create immutable reversing adjustments, reverse the linked journal, require a reason, and reject reversal after later layer changes or consumption; company-scoped create, cursor reads, approval, and reasoned rejection remain available. |
| COGS | Partial | Cost-layer consumption calculates and posts COGS for issues and customer returns; complete reconciliation and return allocation remain. |
| Stock valuation | Partial | Product-level cost-layer valuation report supports product, category, location, batch, and as-of-date filters and derives historical consumption from posted inventory movement dates with a legacy fallback; accounting reconciliation now uses the exact immutable inventory snapshot for the selected date when available, with a date-bounded movement-value fallback; accounting dimensions remain. |
| Inventory adjustment accounting | Partial | Approved adjustment movements post configured inventory/inventory-loss journals; valuation adjustment linkage and reconciliation remain. |
| Cost history | Partial | Product costing changes now create effective-dated history records with actor and old/new values; source-document linkage and historical valuation snapshots remain. |
| Cost method configuration | Partial | Costing method and standard cost can be configured per product; company/class defaults and effective dating remain. |

## 10. Planning and replenishment

| Feature | Status | Target |
|---|---|---|
| Reorder point | Partial | Configure tenant-scoped location-specific reorder points with model-level product/location company consistency and use product-level fallback; proposals now calculate location stock from centralized allocatable availability after reservations and quality holds, honor configured min/max target levels, and are synchronized through authenticated integration APIs, while automatic demand-derived calculation remains. |
| Safety stock | Partial | Maintain location-specific safety-stock values through web and authenticated policy integration APIs; policies can optionally calculate safety stock from the last 90 days of location issues using demand standard deviation, configurable service-level z factor, and lead time, while seasonality and forecast-error methods remain. |
| Min/max levels | Partial | Configure and maintain location-specific min/max levels and lead time through web and authenticated policy integration APIs; automatic replenishment execution remains. |
| Demand forecasting | Partial | Forecast report uses approved sales history with configurable history and horizon windows, optional weekly seasonality, a variability-based 95% confidence band, and explicitly company/location-scoped planner overrides; authenticated forecast integrations can select a tenant-authorized location and derive demand from its immutable outbound ledger, and replenishment feeds can optionally consume the forecast horizon with confidence metadata, while automated model selection remains. |
| Stock replenishment | Partial | Location policies and purchase/production suggestions recommend replenishment; scheduled `erp:planning:generate-purchase-orders` creates auditable supplier-grouped draft purchase orders after an in-transaction, row-locked recheck of open PO demand to prevent duplicate scheduler drafts, while automatic transfer/order execution remains. Procurement integrations now accept idempotent purchase-order and inbound goods-receipt writes with UOM, remaining-quantity, batch/serial, and optional inspection controls. |
| Purchase suggestions | Partial | Supplier-aware suggestions honor active location-specific reorder, safety, min/max, lead-time, and safety-time policies with product-level fallback; the calculation is shared by the browser page, scheduled generator, and tenant-scoped `GET /api/inventory/replenishment` feed using ledger availability and open-PO netting, browser/API planners can optionally apply a demand-forecast horizon with confidence metadata, while selected suggestions can be grouped by supplier into submitted purchase orders with supplier pricing and audit history, and scheduled generation rechecks locked open demand to avoid duplicate drafts; full time-phased lead-time planning remains. |
| Production suggestions | Partial | BOM-aware suggestions, tenant-scoped ledger availability, finished-good netting of open purchase and production supply, component net-supply shortage visibility including outstanding purchase and production supply, direct prefilled production-order creation, and scheduled draft production-order generation are available consistently in browser/API workflows; finite scheduling and automatic release remain. |
| Finite production scheduling | Partial | Draft, released, and in-progress production orders can initialize active routing operations and schedule them through a reusable, locked capacity planner that sequences routing operations, respects active work-center daily capacity, configured shift windows, and weekend/holiday calendars, avoids existing scheduled work-center load, persists planned start/end timestamps, and is available through browser and manufacturing integration workflows; setup optimization, alternate resources, and automatic release remain. |
| Excess stock | Partial | Planning exception reports identify allocatable stock above configured maximum; demand-coverage and value thresholds remain. |
| Slow-moving stock | Partial | Planning reports identify items without recent issue movements using allocatable availability and allow a validated 1–3650 day aging window; configurable velocity rules remain. |
| Dead stock | Partial | Planning reports identify positive allocatable stock without recent issue movements and allow a validated 1–3650 day aging window; configurable aging rules remain. |
| Lead-time planning | Partial | Supplier lead times and configurable location-policy safety-time buffers are stored and included in replenishment proposals and generated purchase-order expected dates; company and supplier planning calendars support configurable weekend days and holiday dates, and working-day receipt dates are used consistently by browser planning and scheduled purchase-order generation; authenticated `GET /api/inventory/replenishment/time-phased` now projects daily demand, open-PO receipts, planned replenishment receipts, balances, reorder/stockout status, and shortfall across a validated 1–365 day horizon. Full multi-echelon and scenario planning remain. |
| Planning dashboard | Partial | Consolidated dashboard shows tenant-scoped low, excess, dead, and expiring inventory with links to exception and MRP reports; dead-stock, expiry, slow-moving, and detailed exception windows can use company ERP settings with validated defaults, while forecasting, aging charts, and action queues remain. |
| Planning integration API | Partial | Authenticated inventory APIs expose database-paginated replenishment proposals and daily time-phased projections with optional forecast-horizon demand and confidence metadata, open purchase receipts, planned receipts, projected balances, reorder/stockout status, and shortfall; tenant-scoped low/excess/slow/dead exception feeds derive quantities from approved ledger availability and accept authorized location filters and policy thresholds, alongside configurable aging and `updated_since` synchronization, plus tenant-authorized policy feeds with deterministic cursor synchronization, idempotent external references, update, and deactivation operations; provider adapters and automatic execution remain. |

## 11. Service and maintenance

| Feature | Status | Target |
|---|---|---|
| Asset | Partial | Register owned/customer assets with company-scoped identifiers, identity, serial, location, warranty, and status; service integrations support company-scoped cursor feeds, idempotent create, update, retirement, effective-dated ownership transfers/history, straight-line, declining-balance, and units-of-production valuation snapshots, plus auditable depreciation journal posting using configured expense and accumulated-depreciation accounts; tax-specific depreciation remains. |
| Equipment | Partial | Equipment can be represented in the service asset register with manufacturer, model, installation date, meter reading, and operational/needs-repair/out-of-service condition; telemetry, meter-triggered maintenance, and richer specifications remain. |
| Spare parts | Partial | Only active stock-managed products can be linked to service assets with per-service quantity, minimum/maximum stock, notes, and maintenance consumption; authenticated service APIs now expose tenant-safe spare-part list/create/update operations with quantity-bound validation and audit events, while automatic replenishment and vendor catalogs remain. |
| Service request | Partial | Create and prioritize service requests with company-scoped identifiers, asset/customer linkage, active-technician assignment, assignment actor/timestamp, audited reassignment, contract validation, contract-derived response deadlines, normalized `sla_status` values (`not_tracked`, `due`, `met`, `breached`, `closed`), and repeat-safe daily SLA-breach notifications; dispatch escalation and richer SLA workflow remain. |
| Maintenance order | Partial | Create preventive/corrective/inspection work orders with company-scoped identifiers, asset-matched service requests, and available-technician validation; audited start, completion, cancellation, outcome, labor hours/cost, asset status, linked service-request resolution, optional idempotent service-labor journal posting, and completed-order customer service invoicing are now supported; scheduling and settlement remain. |
| Parts consumption | Partial | Spare parts can be issued to maintenance orders only when they are active stock-managed products, with location, batch/lot, serial-number, stock validation, serial lifecycle, per-serial ledger posting, cost history, and controlled unused-part returns with correcting ledger entries; authenticated service integrations can consume and return parts with traceability, while approval policy remains. |
| Technician | Partial | Technician profiles now link company-scoped users with employee code, skills, availability, phone, and hourly rate for service assignment; authenticated service APIs expose cursor-based synchronization, idempotent external-reference create, and update operations with availability controls, while schedules, skills certification, and dispatch optimization remain. |
| Warranty | Partial | Service assets store warranty expiry and warranty claims now capture derived in-warranty/expired/unknown coverage status, explicit approval/rejection coverage decisions, issue, status, and resolution notes; authenticated service APIs support company-scoped claim synchronization, idempotent creation, and controlled review/approval/rejection/resolution updates, while vendor warranty, entitlement plans, and claim settlement remain. |
| Service history | Partial | Asset requests, maintenance orders, consumed-part history, labor outcomes, source-linked service invoices, tenant-scoped service contracts with validity, coverage, response SLA, commercial value, automated expiry, audit history, and a consolidated asset-history API are available; settlement and richer contract entitlement automation remain. |
| Employee master | Partial | Company-scoped employees now store employee number, user/branch/department linkage, contact and job data, employment lifecycle, pay frequency, basic salary, and currency; browser administration now includes employee-specific benefit add/deactivate controls, termination audit events, seeded HR permissions, and tenant-safe HR integration feeds. Basic frequency-matched payroll runs generate auditable payslips, totals, approval lifecycle, period-overlap protection, idempotent API creation, attendance deductions, overtime, configurable statutory deduction authorities/remittance account mappings, employer contribution formulas/mappings, employee-specific effective-dated fixed/percentage earnings and deductions, payroll journal posting, and approved-pay-run settlement to a mapped cash/bank account with payment reference and idempotent replay; statutory filing and richer payroll settlement remain. |
| Leave management | Partial | Company-scoped leave types and employee requests support annual entitlements, paid/unpaid classification, calendar-day calculation, overlap protection, active-employee validation, pending/approved/rejected lifecycle, decision reasons, audit events, browser administration, and tenant-safe HR APIs; work schedules, holidays, carry-forward, accruals, and payroll integration remain. |
| Attendance | Partial | Company-scoped daily attendance records support unique employee/date entries, present/absent/leave/holiday statuses, check-in/out, calculated worked minutes, optional scheduled minutes with derived overtime, notes, browser/API recording and filtering, and audit events; payroll runs can explicitly apply recorded absent days as auditable unpaid-absence deductions or pay recorded overtime with a configured multiplier while preserving ignore policies; shift calendars, biometric integration, and correction workflows remain. |
| Preventive schedules | Partial | Recurring asset schedules store calendar cadence or meter interval/next-meter thresholds, due date, assignee, and notes and can generate preventive maintenance orders while advancing triggered thresholds; scheduled `erp:service:generate-due` execution and tenant-safe service APIs for cursor reads, idempotent creation, updates, and guarded generation are now available, while telemetry ingestion remains. |

## 12. Accounting integration

| Feature | Status | Target |
|---|---|---|
| Accounts payable | Partial | Supplier invoices, outstanding balances, aging buckets and collection status using explicit invoice due dates or supplier payment terms, payment approval/rejection, AP journal posting, supplier advances, company-scoped payment allocations with same- and cross-currency conversion snapshots, persisted unallocated/partially-allocated/fully-allocated payment state with backfill, audited allocation voiding, reversal protection for active allocations, controlled payment reversal through web/API, reversal-aware grouped web aging totals, settled supplier-claim credits applied to AP aging, tenant-safe supplier statement and as-of supplier-aging integration feeds, and repeat-safe scheduled database alerts for overdue suppliers are available; supplier statements show each payment and claim settlement once, expose invoice-side and payment-side allocation totals/counts as non-posting metadata, while remittance workflows, formal credit-note journals, and full bank reconciliation remain. |
| Accounts receivable | Partial | Customer credit control now includes allocation-aware outstanding balances that exclude reversed payments and voided allocations; unallocated customer payments can be created, cash/AR-journalized, allocated transactionally to company-scoped approved invoices with same- or cross-currency conversion snapshots, and safely unallocated through audited voiding across approved, customer-linked sales invoices through both the accounting integration API and a permission-protected web screen with idempotent payment/allocation references, persisted unallocated/partially-allocated/fully-allocated payment state with backfill, company-scoped API foreign-key validation, reversal-aware grouped web aging totals, and a tenant-safe customer statement feed with opening/closing running balances for invoices and each payment exactly once while exposing invoice-side and payment-side allocation totals/counts as non-posting metadata; repeat-safe scheduled database reminders for overdue customers are available, while formal receivables ledger and settlement controls remain. |
| Inventory account | Partial | Chart-of-accounts and configurable inventory mappings now enforce company scope while retaining shared fallback mappings; company-specific defaults and reconciliation remain. |
| COGS | Partial | FIFO consumption and automatic COGS posting are available when mappings exist; full sales accounting and reconciliation remain. |
| Tax | Partial | Effective-dated product-linked tax rates now drive API sales and purchase invoice calculations with legacy numeric fallback, while company-scoped approved-document period reporting separates sales output tax and purchase input tax by rate and jurisdiction, nets approved customer-return taxable value/tax using return jurisdiction snapshots, shows the net position, and is available through authenticated accounting `GET /api/accounting/tax-report`; `GET /api/accounting/tax-reconciliation` compares that expected net tax to posted mapped tax-control journal activity with an explicit tolerance and variance status, `POST /api/accounting/tax-settlements` can idempotently remit a positive net position from mapped tax payable to cash/bank with period, jurisdiction, payment reference, and journal auditability, and tax filing snapshots now retain the canonical report payload plus SHA-256 integrity hash, expose verification, and support draft/submitted/accepted/rejected lifecycle with auditable filing references and idempotent creation; direct government portal submission, jurisdiction-specific forms, and exemptions remain. |
| Purchase accounting | Partial | Receipt, purchase-invoice, supplier-payment, source-aware purchase-return journals, and supplier credit-note creation/approval/rejection are available; credit notes can link accepted claims or purchase invoices, validate supplier and remaining claim value, post optional balanced AP recovery journals, apply approved credit amounts to invoice-level AP aging, and appear in supplier statements without double counting claim settlements, while full reconciliation remains. |
| Sales accounting | Partial | Sales invoice approval posts receivable, revenue, tax, and captured payment journals; approved customer returns reverse revenue/tax against receivables and restore inventory/COGS, and approved refund records post receivable-to-cash settlement journals; source-invoice matching, discounts, and reconciliation remain. |
| Stock adjustment accounting | Partial | Approved adjustment movements post configured inventory/inventory-loss journals, with adjustment references retained on the resulting journal entries; authenticated `GET /api/inventory/adjustment-reconciliation` now compares approved adjustment movement value to posted source journals, reports missing journals and variances with a configurable tolerance, and remains read-only; valuation adjustment linkage and richer financial dimensions remain. |
| Cost centers | Partial | Company-scoped cost centers can be administered, journal lines can reference them alongside departments, period budgets can be maintained, and the report compares budget to posted debit activity; budget alerts, allocation rules, and richer budget-versus-actual dashboards remain. |
| Chart of accounts | Partial | Hierarchical company-scoped chart-of-accounts records and administration screens support create, controlled edit, code uniqueness, active-parent validation, descendant-cycle protection, type-change protection after journal use, and mapped-account deactivation safeguards; mapping rules and richer editing remain. |
| Journal entries | Partial | Balanced manual and automated journals, company-scoped entry numbering and external-reference idempotency, account/department/cost-center validation, draft save, maker/checker approval, journal visibility, application-level immutability for posted/reversed entries and lines, auditable one-time reversal through web/API, closed-period date enforcement, and company-scoped recurring journal templates with daily/weekly/monthly idempotent scheduled generation are available; posted-journal trial balance, period Profit & Loss, as-of Balance Sheet, and configured-cash-account Cash Flow reports with browser/API access and balance checks are available; consolidation and richer posting controls remain. |
| Reconciliation | Partial | Reconciliation report compares AP, AR, and inventory subledger balances with posted control-account journals as of a selected date while excluding reversed payments and voided allocations, authorized users can save auditable dated reviews, immutable date-bounded inventory snapshots preserve product/location balances with deterministic hashes and idempotent company/date capture, and tax reconciliation compares approved-document tax to posted mapped tax-control journals with tolerance and variance status; CSV bank statement lines can be imported singly or in atomic batches with auditable provider/actor batch records and line links, batch monitoring, and all-or-nothing rollback, manually matched, or assisted with tenant-safe exact-amount/nearest-date payment suggestions before being unmatched or ignored with audited status transitions; unmatched bank lines can now be settled against a selected company GL account with bank-account GL validation, balanced journal posting, mandatory reason, audit metadata, and replay safety, while live bank connections and controlled journal reversal workflows remain. |
| Accounting integration API | Partial | Sanctum-protected APIs expose active company and shared chart-of-accounts, tenant-safe idempotent account create/update/deactivate synchronization, posted journal, customer/supplier payment, customer/supplier statement, customer-aging, customer credit-assessment, and customer-refund synchronization with allocation, approval, reversal, as-of aging-bucket state, deterministic `(updated_at, id)` incremental ordering and opt-in signed cursor acknowledgement, filtered journal/payment/refund reads, company-scoped idempotent external journal imports, and permission-protected customer/supplier allocation voiding; token-company ownership and model-level accounting scope prevent cross-company access, while provider adapters and richer event feeds remain. |
| Inventory integration API | Partial | Sanctum-protected product/catalog, stock, cost-layer valuation and immutable cost-layer adjustment history, physical stock-count/recount, immutable date-bounded reconciliation snapshots, independent receipt/issue document, immutable movement-ledger with batch/lot/serial traceability, warehouse-transfer, reservation, batch-wise stock, and serial-wise stock endpoints expose company-authorized location-filtered data; product feeds support lifecycle/type/eligibility filters, while product, valuation, cost-adjustment, count, snapshot, document, movement, transfer, adjustment, batch, and serial feeds support product/category/location/batch/as-of/status/reason filters where applicable plus opt-in signed cursor pagination with company/token-scoped acknowledgement at `/api/integration/cursors/{feed}/acknowledge`, alongside backward-compatible `updated_since` pagination, while write access applies company-scoped product, location, adjustment-number, and reservation-destination validation, creates idempotent approval-pending stock adjustments and independent receipt/issue documents using external references, validates department/cost-center and batch/serial context, and supports API inspection plus shared-service approval/rejection with maker/checker, batch/serial validation, ledger posting, accounting, audit, and required rejection reasons, as well as reservation reassignment/release; signed webhook outbox delivery is available, while provider adapters and wider feed coverage remain. |
| Service integration API | Partial | Sanctum-protected asset, service-request, maintenance-order, preventive-schedule, and warranty-claim feeds support company-scoped reads, opt-in signed cursor pagination and acknowledgement, deterministic `(updated_at, id)` incremental ordering, idempotent asset/request/order/schedule/claim references, asset create/update/retirement, ownership transfer/history, straight-line valuation/depreciation posting, spare-part catalog synchronization, schedule create/update/due generation, warranty claim create/review/approve/reject/resolve, request creation, available-technician assignment, asset-matched order creation, controlled start/complete/cancel status transitions with optional labor journal posting, location/batch/serial-aware maintenance-part consumption and controlled returns with return history in the order feed, and audit events; webhooks and provider adapters remain. |
| Sales/procurement integration feeds | Partial | Ability-scoped incremental feeds expose sales orders, deliveries, sales invoices, sales returns, purchase orders, goods receipts, purchase invoices, purchase returns, and supplier claims with customer/supplier, source-document, location, line-product, approval, fulfillment, tax, payment, quality-inspection, and commercial-resolution state, active supplier/customer price agreements, and active named sales/purchase price lists with quantity breaks filtered by date, quantity, currency, and scope; customer/supplier and product master synchronization feeds plus idempotent named price-list and quantity-break writes are available; `sales:write` adds idempotent sales-order creation and approval plus safe cancellation, pending-delivery creation, delivery approval/stock issue, warehouse pick/pack completion, and dispatched-delivery confirmation with tenant validation, UOM normalization, currency, customer-price fallback, credit controls, reservation release, quantity checks, batch/serial capture, and maker/checker workflow preservation; `purchasing:write` adds idempotent supplier purchase-order creation, approval, receipt-aware cancellation, inbound goods-receipt creation from approved PO lines with remaining-quantity checks, UOM/cost normalization, batch/serial/date capture, optional inspection gating, approval-pending status, and reasoned pending-receipt rejection, plus supplier-claim creation and controlled lifecycle updates. All feeds use deterministic `(updated_at, id)` ordering and opt-in signed cursor pagination/acknowledgement, while tenant-scoped webhook subscriptions and internally consistent delivery records support audited activation/deactivation, durable signed outbox deliveries, stable delivery-id idempotency headers, five-attempt retry handling, authorized manual retry of failed deliveries, queued-delivery cancellation, inactive-subscription worker guards, scheduled delivery, and filtered delivery-status monitoring, while provider adapters remain. |

## 13. Audit, security, and governance

| Feature | Status | Target |
|---|---|---|
| Role-based access | Partial | Role/permission middleware now protects legacy masters, purchasing, sales, inventory, accounting, service, planning, report, finance, audit, and security routes; core masters, organization records, commercial documents, sales/purchase documents, adjustments, transfers, movements, reservations, status balances, payments, private attachments, invoice/purchase lines, and journal lines now apply company scope, while line-level policy details and full branch/warehouse policy remain. |
| Permissions | Partial | Module permissions now protect legacy and ERP actions, including inventory, purchasing, sales, warehouse, manufacturing, service, planning, finance, accounting, security, and reports, with dedicated report export permission; write-side integration routes now enforce Sanctum ability guards in addition to controller checks, and assigned branch users are constrained to branch-owned warehouse/store/location reads; complete view/create/update/delete/print matrix remains. |
| Approval workflow | Partial | Company-scoped approval policies now support document type, amount ranges, branch/category routing, ordered multi-step permissions, persisted approver actions, active state, optional escalation-after-hours SLAs, administration UI, overlap protection for active same-step scopes, and shared enforcement across browser and integration approvals for sales/purchase orders, requisitions, quotations, receipts, invoices, returns, payments, landed costs, journals, counts, transfers, adjustments, and status changes; policies and date-bounded, document-scoped, overlap-protected, audited delegations are manageable through tenant-scoped integration APIs, while escalation tiers remain. |
| Maker/checker | Partial | Approval guard prevents a document creator from approving controlled adjustment, transfer, return, count, status, payable, landed-cost, and manual journal documents, including when that creator is acting through a temporary delegation; delegated authority now requires an active same-company delegator with the required permission, while configurable exception policies remain. |
| Audit trail | Partial | ERP audit records capture actor, action, record, old/new values, IP, and user agent and are searchable/exportable by action, user, and date with tenant-scoped audit/history/activity retrieval; external systems can consume a filtered deterministic cursor audit-log feed; new events are tamper-evident through company-serialized SHA-256 chaining, hash columns in audit exports, and `erp:security:verify-audit-chain`, while archival remains. |
| Transaction history | Partial | Immutable document status transitions are recorded automatically alongside audit events and are searchable by document/date with tenant-scoped retrieval; full event sourcing and document revision UI remain. |
| User activity | Partial | Login, logout, and failed-login activity is recorded and searchable with actor/email, route, IP, user agent, metadata, and tenant scope; filtered CSV export is permission-protected, while print, approval, retention, and sensitive-action coverage remain. |
| Data versioning | Partial | Audited master and transaction changes now create tenant-scoped versioned before/after snapshots with actor and audit linkage; a permission-protected tenant-scoped JSON field-diff endpoint is available, while restore and full field coverage remain. |
| Soft delete | Partial | Core product, supplier, customer, unit, category, purchase, invoice, payment, organization, and location records use soft deletes; a permission-protected allowlisted restore endpoint now verifies direct company ownership or branch/warehouse/location ownership before restoring and records audit history, while restore UI and dependency-aware restoration remain. |
| Document attachments | Partial | Secure private attachments now cover products, purchasing, receiving, sales, deliveries, returns, warehouse documents, stock counts, landed costs, manufacturing, and service records with MIME/size validation, type-specific module authorization, attachable-record ownership checks, explicit company ownership on new records, authenticated web/API download from the explicit local private disk, audit logging, company-scoped retrieval, monotonic per-record document/image versions, and tenant-safe cursor-paginated integration metadata plus idempotent multipart upload at `GET/POST /api/inventory/attachments` with type, record, attachment-kind, and `updated_since` filters; inline attachment widgets for individual ERP screens and retention policies remain. |
| Session/security controls | Partial | Login throttling, configurable password complexity and ten-password reuse prevention for registration/reset/change, persistent hashed session tracking, device/IP review, session revocation, TOTP MFA enrollment/challenge enforcement, encrypted recovery codes, recovery-code consumption, and reasoned company-scoped administrative MFA reset with API-token revocation are available; password rotation and breach screening remain. |
| Segregation of duties | Partial | Administrators can define incompatible role pairs and assignment is blocked when a user already holds a conflicting role; transaction-level policy simulation and override workflow remain. |
| Data retention | Partial | Retention policies, legal holds, previews, archive browsing, and CSV export now enforce explicit current-company ownership, while underlying legacy audit/revision records may remain nullable when reached through an authorized tenant policy; archival snapshots and scheduled command execution are available, while purge approvals and external immutable storage remain. |

## 14. Reports and dashboard

| Feature | Status | Target |
|---|---|---|
| Current stock | Partial | Current-stock report, inventory API, and centralized availability checks now derive on-hand consistently from approved ledger movements rather than the legacy product quantity, support product/category/location filters, and display location-aware on-hand (including quarantine release/hold movements), reserved, quality-hold, and allocatable available quantities; batch and serial drilldowns remain available in dedicated reports. |
| Stock ledger | Partial | Movement report shows movement type, product, quantity, cost, source, location, actor, date, and date-bounded opening/closing balances; immutable tenant-scoped product/location reconciliation snapshots with movement totals, negative-balance variance status, deterministic hashes, daily scheduled capture, and idempotent reruns are available, while period close remains. |
| Stock valuation | Partial | FIFO/cost-layer valuation reports item values and costing methods, while inventory analytics and web/PDF stock reports now use approved ledger balances for products with movement history and retain legacy fallback only for unmigrated products; historical date, location, batch, and accounting dimensions remain. |
| Batch expiry | Partial | Expiry report shows upcoming and expired batches with dates, ledger-derived quantities, and value-at-risk and supports product/location/updated-since filtering; expiry notifications now carry the same cost-layer-backed value-at-risk metric, while override approvals remain. |
| Low stock | Partial | Report items at or below product reorder level using allocatable availability; repeat-safe scheduled database notifications now alert authorized users using the same availability basis, while recommendation generation remains. |
| Overstock | Partial | Browser and integration planning reports identify items above configured maximum stock and now expose excess quantity plus purchase-cost excess value; coverage and value thresholds remain. |
| Slow-moving items | Partial | Browser and integration planning reports identify items without recent issue movements using a validated configurable 1–3650 day window (90 days by default); configurable velocity rules remain. |
| Dead stock | Partial | Report positive stock without recent issue movements using a validated configurable 1–3650 day window; configurable aging remains. |
| Purchase report | Partial | Supplier, order, receipt, invoice, tax, aging, and purchase-price variance reporting are available; richer period comparison and settlement analytics remain. |
| Sales report | Partial | Approved legacy and ERP invoices can now be filtered by period, tenant-authorized product/customer, with quantity, revenue, tax, ledger-backed COGS where posted issues exist (legacy purchase-price fallback), gross margin, and a machine-readable sales-performance feed that nets approved customer returns; channel, deliveries, salesperson, and export remain. |
| Warehouse report | Partial | Location stock and traceability reports show ledger activity by warehouse location; an authenticated warehouse feed summarizes occupied quantity, quantity capacity, configured weight/volume capacity and availability, and optional period inbound/outbound/net movement by tenant warehouse, while reconciliation remains. |
| Product profitability | Partial | Inventory analytics reports net revenue, ledger-backed COGS, gross profit, margin percentage, net quantity sold, and cost-layer-backed closing inventory value with compatibility fallback for products without layers, plus category/period filters and filtered CSV export; discounts and customer dimensions remain. |
| Inventory turnover | Partial | Inventory analytics derives closing and opening/average stock from the movement ledger (with compatibility fallback for products without ledger history) and reports period quantity-sold turnover plus days of inventory by item, category, and overall summary, with category filtering; configurable segmentation remains. |
| ABC analysis | Partial | Inventory analytics classifies active products A/B/C by cumulative revenue, movement quantity, or current inventory value contribution (`abc_basis`), including positive stock without sales for value analysis; company-configurable A/B thresholds, category filtering, and browser/API/export basis selection are available, while richer multi-dimensional segmentation remains. |
| Customer aging | Partial | Receivables aging derives invoice balances from direct payments and cross-invoice allocations, applies customer payment terms to due dates, exposes due date, overdue age, bucket, and collection status in the web screen/export and tenant-safe API feed, supports as-of/customer filtering, and sends repeat-safe daily overdue reminders through `erp:receivables:overdue-alerts`; settlement controls remain. |
| Supplier performance | Partial | Supplier performance report exposes delivery timeliness, fulfillment, ordered value, invoiced value, approved-invoice average price variance, receipt-inspection quality/rejection metrics, and a transparent weighted score from procurement history; corrective actions and richer cost variance analytics remain. |
| Export | Partial | Product, stock-ledger, inventory-analytics, sales-analysis, tax-reconciliation, receivables-aging, supplier-aging, and supplier-performance CSV exports are permission-protected and retain filters; inventory analytics and product reports also support XLSX output, while broader report export controls remain. |
| Dashboard KPIs | Partial | Dashboard cards query approved monthly sales, location-policy-aware reorder/excess inventory health, pending approvals, allocation-aware outstanding receivables, open service requests, breached service-request SLAs, and active maintenance workload through a company-scoped configurable cache (`ERP_DASHBOARD_CACHE_TTL`, default 60 seconds; `0` disables caching); the dashboard integration response also publishes active planning windows, capacity-alert threshold, and ABC thresholds so external consumers can interpret KPI counts, while cached trends, exception drilldowns, and role-scoped KPIs remain. |

## Technical foundation required

The following work should be completed before implementing advanced modules:

1. Create an immutable inventory movement ledger and derive balances from approved movements.
2. Add company, branch, warehouse, store, and location relationships.
3. Replace monetary `double` fields with fixed-precision decimal fields. Migration `2026_09_11_000117` converts legacy product, purchase, invoice-detail, and payment quantity/amount columns and adds matching model casts.
4. Add foreign keys, indexes, uniqueness rules, and database-level constraints.
5. Add roles, permissions, approval policies, and maker/checker enforcement.
6. Add soft deletes and audit/version history for business records.
7. Introduce document status machines instead of only pending/approved flags.
8. Add tax, currency, fiscal-period, and numbering-sequence foundations.
9. Add automated feature tests using a working isolated test database.
10. Replace static dashboard values with computed queries and cached reporting services.
11. Add validation for dates, quantities, prices, discounts, references, and cross-record ownership.
12. Establish document attachment storage, access control, backup, and retention policies.

## Recommended implementation phases

### Phase 1 — Control and data integrity

- Inventory ledger
- Database foreign keys and indexes
- Decimal money/quantity fields
- Roles and permissions
- Audit trail and soft deletes
- Dynamic dashboard KPIs
- Automated tests and test database configuration

### Phase 2 — Operational inventory

- Company/branch/warehouse/store hierarchy
- Locations, bins, transfers, adjustments, returns, reservations
- Opening stock and physical stocktake
- Batch, serial, expiry, FEFO/FIFO support
- Barcode and SKU support

### Phase 3 — Procurement and fulfillment

- Requisitions, RFQs, quotations, purchase orders
- Goods receipt and supplier invoice matching
- Sales quotations and orders
- Picking, packing, delivery, and returns
- Supplier/customer pricing and promotions

### Phase 4 — Costing, planning, and reporting

- FIFO/weighted-average/standard costing
- COGS and stock valuation
- Reorder planning and purchase suggestions
- Slow/dead/excess stock analysis
- Profitability, turnover, ABC, and advanced exports

### Phase 5 — ERP extensions

- Accounting integration
- Manufacturing, BOM, routing, MRP, WIP
- Assets, maintenance, service, warranties
- Advanced integrations and notifications

## Definition of done for a feature

Each feature should include:

- Database migration and relationships.
- Server-side validation and authorization.
- UI workflow and appropriate status transitions.
- Audit/history behavior.
- Report/filter/export support where relevant.
- Error handling and transactional consistency.
- Automated feature tests.
- Documentation and deployment notes.
