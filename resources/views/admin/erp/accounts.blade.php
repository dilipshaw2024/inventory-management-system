@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-sm-flex align-items-center justify-content-between"><div><h4>Chart of Accounts</h4><p class="text-muted mb-0">Maintain the account structure used by ERP journals.</p></div><a href="{{ route('erp.accounting.journals') }}" class="btn btn-light">View journals</a></div>
<div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('erp.accounting.accounts.store') }}">@csrf<div class="row g-2"><div class="col-md-2"><input name="code" placeholder="Code" class="form-control" required></div><div class="col-md-3"><input name="name" placeholder="Account name" class="form-control" required></div><div class="col-md-2"><select name="account_type" class="form-select" required><option value="asset">Asset</option><option value="liability">Liability</option><option value="equity">Equity</option><option value="income">Income</option><option value="expense">Expense</option></select></div><div class="col-md-3"><select name="parent_id" class="form-select"><option value="">No parent</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} — {{ $account->name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Create account</button></div></div></form></div></div>
<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Code</th><th>Name</th><th>Type</th><th>Parent</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($accounts as $account)<tr><td>{{ $account->code }}</td><td>{{ $account->name }}</td><td>{{ ucfirst($account->account_type) }}</td><td>{{ data_get($account, 'parent.code', '—') }}</td><td>{{ $account->is_active ? 'Active' : 'Inactive' }}</td><td><details><summary class="btn btn-sm btn-outline-secondary">Edit</summary><form method="POST" action="{{ route('erp.accounting.accounts.update', $account->id) }}" class="row g-2 mt-2">@csrf @method('PUT')<div class="col-12"><input name="code" value="{{ $account->code }}" class="form-control form-control-sm" required></div><div class="col-12"><input name="name" value="{{ $account->name }}" class="form-control form-control-sm" required></div><div class="col-12"><select name="account_type" class="form-select form-select-sm"><option value="asset" @selected($account->account_type === 'asset')>Asset</option><option value="liability" @selected($account->account_type === 'liability')>Liability</option><option value="equity" @selected($account->account_type === 'equity')>Equity</option><option value="income" @selected($account->account_type === 'income')>Income</option><option value="expense" @selected($account->account_type === 'expense')>Expense</option></select></div><div class="col-12"><select name="parent_id" class="form-select form-select-sm"><option value="">No parent</option>@foreach($accounts as $parent)<option value="{{ $parent->id }}" @selected($account->parent_id == $parent->id)>{{ $parent->code }} — {{ $parent->name }}</option>@endforeach</select></div><div class="col-12 form-check"><input name="is_active" value="1" type="checkbox" class="form-check-input" id="account-active-{{ $account->id }}" @checked($account->is_active)><label for="account-active-{{ $account->id }}" class="form-check-label">Active</label></div><div class="col-12"><button class="btn btn-sm btn-primary">Update</button></div></form>@if($account->is_active)<form method="POST" action="{{ route('erp.accounting.accounts.deactivate', $account->id) }}" class="mt-1">@csrf<button class="btn btn-sm btn-outline-danger">Deactivate</button></form>@endif</details></td></tr>@empty<tr><td colspan="6" class="text-center">No accounts configured.</td></tr>@endforelse</tbody></table></div>{{ $accounts->links() }}</div></div></div></div>
@endsection
