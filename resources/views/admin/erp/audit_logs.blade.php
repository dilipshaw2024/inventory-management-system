@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Audit Trail</h4><p class="text-muted">Immutable transaction and security activity history.</p></div><div class="card mb-3"><div class="card-body"><form method="GET" class="row g-2 align-items-end"><div class="col-md-3"><label>Action</label><input name="action" value="{{ request('action') }}" class="form-control" placeholder="e.g. approved"></div><div class="col-md-3"><label>User</label><select name="user_id" class="form-select"><option value="">All users</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected(request('user_id') == $user->id)>{{ $user->name }}</option>@endforeach</select></div><div class="col-md-2"><label>From</label><input name="from" type="date" value="{{ request('from') }}" class="form-control"></div><div class="col-md-2"><label>To</label><input name="to" type="date" value="{{ request('to') }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary">Filter</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Timestamp</th><th>User</th><th>Action</th><th>Auditable record</th><th>IP</th><th>Changes</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ optional($log->created_at)->format('Y-m-d H:i:s') }}</td><td>{{ data_get($log, 'user.name', 'System') }}</td><td>{{ $log->action }}</td><td>{{ $log->auditable_type ? class_basename($log->auditable_type).' #'.$log->auditable_id : '—' }}</td><td>{{ $log->ip_address ?? '—' }}</td><td><details><summary>View</summary><small>Old: {{ json_encode($log->old_values) }}<br>New: {{ json_encode($log->new_values) }}</small></details></td></tr>@empty<tr><td colspan="6" class="text-center">No audit events found.</td></tr>@endforelse</tbody></table></div>{{ $logs->links() }}</div></div></div></div>
<div class="mt-3"><a class="btn btn-outline-secondary" href="{{ route('erp.security.audit.export', request()->query()) }}">Export filtered CSV</a></div>
@endsection
