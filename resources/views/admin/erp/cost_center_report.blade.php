@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Cost Center Report</h4><p class="text-muted">Review posted journal activity and budget variance by accounting dimension.</p></div><div class="card mb-3"><div class="card-body"><form method="GET" action="{{ route('erp.accounting.cost-centers.report') }}" class="row g-2"><div class="col-md-3"><label class="form-label">From</label><input type="date" name="from" value="{{ request('from') }}" class="form-control"></div><div class="col-md-3"><label class="form-label">To</label><input type="date" name="to" value="{{ request('to') }}" class="form-control"></div><div class="col-md-4"><label class="form-label">Cost center</label><select name="cost_center_id" class="form-select"><option value="">All cost centers</option>@foreach($costCenters as $center)<option value="{{ $center->id }}" @selected((string) request('cost_center_id') === (string) $center->id)>{{ $center->code }} — {{ $center->name }}</option>@endforeach</select></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Filter</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Cost center</th><th>Budget</th><th>Posted debit</th><th>Variance</th><th>Credit</th><th>Net</th><th>Lines</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ data_get($row, 'cost_center.code', 'Unassigned') }} @if(data_get($row, 'cost_center.name')) — {{ data_get($row, 'cost_center.name') }} @endif</td><td>{{ number_format($row['budget'], 2) }}</td><td>{{ number_format($row['debit'], 2) }}</td><td class="{{ $row['variance'] < 0 ? 'text-danger' : 'text-success' }}">{{ number_format($row['variance'], 2) }}</td><td>{{ number_format($row['credit'], 2) }}</td><td>{{ number_format($row['net'], 2) }}</td><td>{{ $row['lines']->count() }}</td></tr>@empty<tr><td colspan="7" class="text-center">No budget or posted journal activity found.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
