@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Cost Centers</h4><p class="text-muted">Maintain company-scoped accounting dimensions for journal reporting.</p></div><div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('erp.accounting.cost-centers.store') }}" class="row g-2">@csrf<div class="col-md-3"><input name="code" class="form-control" placeholder="Code" required></div><div class="col-md-5"><input name="name" class="form-control" placeholder="Name" required></div><div class="col-md-2"><select name="is_active" class="form-select"><option value="1">Active</option><option value="0">Inactive</option></select></div><div class="col-md-2"><button class="btn btn-primary w-100">Add cost center</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Code</th><th>Name</th><th>Status</th><th>Update</th></tr></thead><tbody>@forelse($costCenters as $center)<tr><form method="POST" action="{{ route('erp.accounting.cost-centers.update', $center->id) }}">@csrf @method('PUT')<td><input name="code" value="{{ $center->code }}" class="form-control form-control-sm"></td><td><input name="name" value="{{ $center->name }}" class="form-control form-control-sm"></td><td><select name="is_active" class="form-select form-select-sm"><option value="1" @selected($center->is_active)>Active</option><option value="0" @selected(!$center->is_active)>Inactive</option></select></td><td><button class="btn btn-sm btn-outline-primary">Save</button></td></form></tr>@empty<tr><td colspan="4" class="text-center">No cost centers configured.</td></tr>@endforelse</tbody></table></div>{{ $costCenters->links() }}</div></div></div></div>
@endsection
