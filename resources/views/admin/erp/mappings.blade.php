@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Automatic Account Mappings</h4><p class="text-muted">Configure accounts used for automatic inventory, payable, payroll, and foreign-currency journals.</p></div><div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('erp.accounting.mappings.store') }}">@csrf<div class="row g-2"><div class="col-md-4"><select name="mapping_key" class="form-select" required><option value="">Select mapping</option>@foreach(['inventory','grni','cogs','inventory_loss','landed_cost_clearing','accounts_payable','accounts_receivable','sales_revenue','sales_tax','cash_bank','cash','bank','fx_gain','fx_loss','payroll_expense','payroll_payable','payroll_deductions'] as $key)<option value="{{ $key }}">{{ ucwords(str_replace('_', ' ', $key)) }}</option>@endforeach</select></div><div class="col-md-5"><select name="account_id" class="form-select" required><option value="">Select account</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} — {{ $account->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-primary w-100">Save mapping</button></div></div></form></div></div><div class="card"><div class="card-body"><table class="table table-bordered"><thead><tr><th>Mapping</th><th>Account</th></tr></thead><tbody>@forelse($mappings as $mapping)<tr><td>{{ ucwords(str_replace('_', ' ', $mapping->mapping_key)) }}</td><td>{{ data_get($mapping, 'account.code', 'N/A') }} — {{ data_get($mapping, 'account.name', 'N/A') }}</td></tr>@empty<tr><td colspan="2" class="text-center">No mappings configured.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
