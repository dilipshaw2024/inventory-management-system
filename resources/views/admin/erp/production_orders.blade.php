@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><div><h4>Production Orders</h4><p class="text-muted">Release orders after component availability checks, then post finished goods.</p></div><div><a href="{{ route('manufacturing.boms') }}" class="btn btn-light">BOMs</a> <a href="{{ route('manufacturing.orders.create') }}" class="btn btn-primary">New order</a></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Order</th><th>Product</th><th>Planned</th><th>Date</th><th>Status</th><th>Material cost</th><th>Operation cost</th><th>By-product</th><th>Net cost</th><th>Actions</th></tr></thead><tbody>@forelse($orders as $order)<tr><td>{{ $order->order_no }}</td><td>{{ $order->product->name }}</td><td>{{ $order->planned_quantity }}</td><td>{{ optional($order->planned_date)->format('Y-m-d') }}</td><td><span class="badge bg-secondary">{{ ucfirst(str_replace('_', ' ', $order->status)) }}</span></td><td>{{ number_format((float) $order->material_cost, 2) }}</td><td>{{ number_format((float) $order->operation_cost, 2) }}</td><td>{{ number_format((float) $order->byproduct_cost, 2) }}</td><td>{{ number_format((float) $order->production_cost, 2) }}</td><td>@if($order->status === 'draft')<form method="POST" action="{{ route('manufacturing.orders.release', $order->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-primary">Release</button></form>@elseif(in_array($order->status, ['released','in_progress']))<form method="POST" action="{{ route('manufacturing.orders.complete', $order->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success">Complete</button></form>@else — @endif</td></tr>@empty<tr><td colspan="10" class="text-center">No production orders.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div></div></div></div>
<div class="card mt-3"><div class="card-body"><h5>Cancellation</h5><p class="text-muted">Open production orders can be cancelled; component reservations are released.</p><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Order</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($orders as $openOrder)@if(in_array($openOrder->status, ['draft', 'released', 'in_progress'], true))<tr><td>{{ $openOrder->order_no }}</td><td>{{ ucfirst($openOrder->status) }}</td><td><form method="POST" action="{{ route('manufacturing.orders.cancel', $openOrder->id) }}" class="d-inline">@csrf<input name="cancellation_reason" value="Cancelled by operator" class="form-control form-control-sm d-inline-block" style="width:170px" required maxlength="2000"><button class="btn btn-outline-danger btn-sm">Cancel</button></form></td></tr>@endif @endforeach</tbody></table></div></div></div>
@include('admin.erp.production_schedule_actions')
@include('admin.erp.production_partial_receipts')
@include('admin.erp.production_operation_links')
@endsection
