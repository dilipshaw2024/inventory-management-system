<div class="card mt-3"><div class="card-body"><h5>Production receipt</h5><p class="text-muted">Receive a partial quantity; the order stays in progress until the planned quantity is reached.</p><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Order</th><th>Remaining</th><th>Quantity to receive</th><th>Action</th></tr></thead><tbody>@foreach($orders as $receiptOrder)@if(in_array($receiptOrder->status, ['released', 'in_progress'], true))<tr><td>{{ $receiptOrder->order_no }} — {{ $receiptOrder->product->name }}</td><td>{{ number_format(max(0, (float) $receiptOrder->planned_quantity - (float) $receiptOrder->completed_quantity), 6) }}</td><td><form method="POST" action="{{ route('manufacturing.orders.complete', $receiptOrder->id) }}" class="d-flex gap-1">@csrf<input name="produced_quantity" type="number" min="0.000001" max="{{ max(0, (float) $receiptOrder->planned_quantity - (float) $receiptOrder->completed_quantity) }}" step="0.000001" value="{{ max(0, (float) $receiptOrder->planned_quantity - (float) $receiptOrder->completed_quantity) }}" class="form-control form-control-sm" required><button class="btn btn-sm btn-outline-success">Receive</button></form></td><td></td></tr>@endif @endforeach</tbody></table></div></div></div>
