<div class="card mt-3"><div class="card-body"><h5>Finite scheduling</h5><p class="text-muted">Schedule pending production operations against work-center capacity, calendars, and existing scheduled load.</p><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Order</th><th>Status</th><th>Scheduled operations</th><th>Action</th></tr></thead><tbody>@foreach($orders as $scheduleOrder)@if(in_array($scheduleOrder->status, ['draft', 'released', 'in_progress'], true))<tr><td>{{ $scheduleOrder->order_no }}</td><td>{{ ucfirst($scheduleOrder->status) }}</td><td>{{ $scheduleOrder->operations->where('schedule_status', 'scheduled')->count() }} / {{ $scheduleOrder->operations->count() }}</td><td><form method="POST" action="{{ route('manufacturing.orders.schedule', $scheduleOrder->id) }}" class="d-inline">@csrf<button class="btn btn-outline-info btn-sm">Schedule</button></form></td></tr>@endif @endforeach</tbody></table></div></div></div>
