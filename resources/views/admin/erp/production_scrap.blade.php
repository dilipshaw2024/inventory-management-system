@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><div><h4>Production Scrap</h4><p class="text-muted">Review approved production scrap postings and their ledger traceability.</p></div><a href="{{ route('manufacturing.scrap.create') }}" class="btn btn-primary">Record scrap</a></div>
<div class="row g-3 mb-3"><div class="col-md-6"><div class="card mb-0"><div class="card-body"><small class="text-muted">Approved scrap quantity</small><h4>{{ number_format((float) $scrapTotals->quantity, 3) }}</h4></div></div></div><div class="col-md-6"><div class="card mb-0"><div class="card-body"><small class="text-muted">Approved scrap value</small><h4>{{ number_format((float) $scrapTotals->scrap_value, 2) }}</h4></div></div></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Order</th><th>Product</th><th>Quantity</th><th>Location</th><th>Reason</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($scrap as $record)<tr><td>{{ $record->productionOrder?->order_no }}</td><td>{{ $record->product?->name }}</td><td>{{ $record->quantity }}</td><td>{{ $record->location?->code ?: 'Global' }}</td><td>{{ $record->reason }}</td><td>{{ ucfirst($record->status) }}</td><td>@if($record->status === 'pending')<form method="POST" action="{{ route('manufacturing.scrap.approve', $record->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success">Approve</button></form><form method="POST" action="{{ route('manufacturing.scrap.reject', $record->id) }}" class="d-inline">@csrf<input type="hidden" name="reason" value="Rejected by reviewer"><button class="btn btn-sm btn-outline-danger">Reject</button></form>@else — @endif</td></tr>@empty<tr><td colspan="7" class="text-center">No production scrap records.</td></tr>@endforelse</tbody></table></div>{{ $scrap->links() }}</div></div></div></div>
@endsection
