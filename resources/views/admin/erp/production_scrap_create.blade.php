@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Record Production Scrap</h4><p class="text-muted">The record remains pending until an authorized checker approves it.</p></div>
<div class="card"><div class="card-body"><form method="POST" action="{{ route('manufacturing.scrap.store') }}">@csrf<div class="row g-3"><div class="col-md-6"><label>Production order</label><select name="production_order_id" class="form-select" required><option value="">Select order</option>@foreach($orders as $order)<option value="{{ $order->id }}">{{ $order->order_no }} — {{ $order->product?->name }} ({{ $order->status }})</option>@endforeach</select></div><div class="col-md-6"><label>Product</label><select name="product_id" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }} ({{ $product->sku }})</option>@endforeach</select></div><div class="col-md-4"><label>Quantity</label><input name="quantity" type="number" min="0.000001" step="0.000001" class="form-control" required></div><div class="col-md-4"><label>Unit cost</label><input name="unit_cost" type="number" min="0" step="0.000001" class="form-control"></div><div class="col-md-4"><label>Location</label><select name="location_id" class="form-select"><option value="">Global / legacy stock</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->code }} — {{ $location->name }}</option>@endforeach</select></div><div class="col-md-6"><label>Batch number (optional)</label><input name="batch_no" class="form-control" maxlength="100"></div><div class="col-md-6"><label>Serial numbers (optional)</label><input name="serial_numbers" class="form-control" placeholder="Comma or newline separated"></div><div class="col-md-4"><label>Recovery product (optional)</label><select name="recovery_product_id" class="form-select"><option value="">No recovery</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></div><div class="col-md-4"><label>Recovery quantity</label><input name="recovery_quantity" type="number" min="0.000001" step="0.000001" class="form-control"></div><div class="col-md-4"><label>Recovery unit value</label><input name="recovery_unit_cost" type="number" min="0" step="0.000001" class="form-control"></div><div class="col-12"><label>Reason</label><textarea name="reason" class="form-control" required maxlength="2000"></textarea></div><div class="col-12"><button class="btn btn-primary">Submit for approval</button> <a href="{{ route('manufacturing.scrap') }}" class="btn btn-light">Cancel</a></div></div></form></div></div></div></div>
@endsection
