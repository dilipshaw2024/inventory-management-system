@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Document Status History</h4><p class="text-muted">Immutable workflow transitions recorded across ERP documents.</p></div><div class="card mb-3"><div class="card-body"><form class="row g-2"><div class="col-md-3"><input name="document_type" value="{{ request('document_type') }}" class="form-control" placeholder="Document type"></div><div class="col-md-2"><input name="document_id" value="{{ request('document_id') }}" class="form-control" placeholder="Document ID"></div><div class="col-md-2"><input type="date" name="from" value="{{ request('from') }}" class="form-control"></div><div class="col-md-2"><input type="date" name="to" value="{{ request('to') }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary">Filter</button></div></form></div></div><div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Changed</th><th>Document</th><th>Transition</th><th>Action</th><th>User</th></tr></thead><tbody>@forelse($histories as $history)<tr><td>{{ optional($history->changed_at)->format('Y-m-d H:i:s') }}</td><td>{{ class_basename($history->document_type) }} #{{ $history->document_id }}</td><td>{{ $history->from_status ?? '—' }} → {{ $history->to_status }}</td><td>{{ $history->action ?? '—' }}</td><td>{{ data_get($history, 'user.name', 'System') }}</td></tr>@empty<tr><td colspan="5" class="text-center">No status transitions found.</td></tr>@endforelse</tbody></table>{{ $histories->links() }}</div></div></div></div>
@endsection
