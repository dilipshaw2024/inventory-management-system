@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid">
    <div class="page-title-box"><h4>Trial Balance</h4><p class="text-muted">Posted journal balances by account for the selected period.</p></div>
    <div class="card mb-3"><div class="card-body"><form method="GET" class="row g-2"><div class="col-md-3"><label class="form-label">From</label><input type="date" name="from" value="{{ $from }}" class="form-control"></div><div class="col-md-3"><label class="form-label">To</label><input type="date" name="to" value="{{ $to }}" class="form-control"></div><div class="col-md-4"><label class="form-label">Account</label><select name="account_id" class="form-select"><option value="">All accounts</option>@foreach($accounts as $account)<option value="{{ $account->id }}" @selected(request('account_id') == $account->id)>{{ $account->code }} — {{ $account->name }}</option>@endforeach</select></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Filter</button></div></form></div></div>
    <div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Account</th><th>Type</th><th>Opening debit</th><th>Opening credit</th><th>Period debit</th><th>Period credit</th><th>Closing debit</th><th>Closing credit</th><th>Net balance</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row['code'] }} — {{ $row['name'] }}</td><td>{{ ucfirst($row['account_type']) }}</td><td>{{ number_format($row['opening_debit'], 2) }}</td><td>{{ number_format($row['opening_credit'], 2) }}</td><td>{{ number_format($row['period_debit'], 2) }}</td><td>{{ number_format($row['period_credit'], 2) }}</td><td>{{ number_format($row['closing_debit'], 2) }}</td><td>{{ number_format($row['closing_credit'], 2) }}</td><td>{{ number_format($row['net_balance'], 2) }}</td></tr>@empty<tr><td colspan="9" class="text-center">No posted journal activity found.</td></tr>@endforelse</tbody></table></div></div></div>
</div></div>
@endsection
