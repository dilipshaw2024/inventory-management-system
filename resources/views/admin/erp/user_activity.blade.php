@extends('admin.admin_master')
@section('admin')
<div class="container-fluid mt-2 text-end"><a class="btn btn-outline-secondary" href="{{ route('erp.security.audit.activity.export', request()->query()) }}">Export filtered CSV</a></div>
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>User Activity</h4><p class="text-muted">Authentication activity including successful, failed, and logout events.</p></div><div class="card mb-3"><div class="card-body"><form class="row g-2"><div class="col-md-3"><input name="action" value="{{ request('action') }}" class="form-control" placeholder="login, logout"></div><div class="col-md-3"><select name="user_id" class="form-select"><option value="">All users</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected(request('user_id') == $user->id)>{{ $user->name }}</option>@endforeach</select></div><div class="col-md-2"><input name="from" type="date" value="{{ request('from') }}" class="form-control"></div><div class="col-md-2"><input name="to" type="date" value="{{ request('to') }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Timestamp</th><th>User</th><th>Email</th><th>Action</th><th>Route</th><th>IP</th><th>User agent</th></tr></thead><tbody>@forelse($activities as $activity)<tr><td>{{ optional($activity->created_at)->format('Y-m-d H:i:s') }}</td><td>{{ data_get($activity, 'user.name', 'Guest') }}</td><td>{{ $activity->email ?: '—' }}</td><td>{{ $activity->action }}</td><td>{{ $activity->route ?: '—' }}</td><td>{{ $activity->ip_address ?: '—' }}</td><td><small>{{ \Illuminate\Support\Str::limit($activity->user_agent, 80) }}</small></td></tr>@empty<tr><td colspan="7" class="text-center">No user activity found.</td></tr>@endforelse</tbody></table></div>{{ $activities->links() }}</div></div></div></div>
@endsection
