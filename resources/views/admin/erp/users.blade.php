@extends('admin.admin_master')
@section('admin')
<div class="container-fluid mt-2"><div class="card border-warning"><div class="card-body"><h5>Reset user MFA</h5><form method="POST" action="{{ route('erp.security.users.reset-mfa', 0) }}" onsubmit="this.action=this.action.replace('/0/reset-mfa','/'+this.user_id.value+'/reset-mfa');"><div class="row g-2"><div class="col-md-4"><select name="user_id" class="form-select" required><option value="">Select user</option>@foreach($users as $managedUser)<option value="{{ $managedUser->id }}">{{ $managedUser->name }} — {{ $managedUser->email }}</option>@endforeach</select></div><div class="col-md-6"><input name="reason" class="form-control" placeholder="Reason for administrative reset" required maxlength="2000"></div><div class="col-md-2"><button class="btn btn-warning w-100">Reset MFA</button></div></div>@csrf</form></div></div></div>
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>User Administration</h4><p class="text-muted">Manage activation and organizational scope without deleting user history.</p></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>User</th><th>Company</th><th>Branch</th><th>Department</th><th>Active</th><th>Roles</th><th></th></tr></thead><tbody>@forelse($users as $user)<tr><form method="POST" action="{{ route('erp.security.users.update', $user->id) }}">@csrf @method('PUT')<td>{{ $user->name }}<br><small>{{ $user->email }}</small></td><td><select name="company_id" class="form-select"><option value="">—</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected($user->company_id == $company->id)>{{ $company->name }}</option>@endforeach</select></td><td><select name="branch_id" class="form-select"><option value="">—</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected($user->branch_id == $branch->id)>{{ $branch->name }}</option>@endforeach</select></td><td><select name="department_id" class="form-select"><option value="">—</option>@foreach($departments as $department)<option value="{{ $department->id }}" @selected($user->department_id == $department->id)>{{ $department->name }}</option>@endforeach</select></td><td><input type="checkbox" name="is_active" value="1" @checked($user->is_active)> </td><td>{{ $user->roles->pluck('name')->join(', ') ?: '—' }}</td><td><button class="btn btn-sm btn-primary">Save</button></td></form></tr>@empty<tr><td colspan="7" class="text-center">No users.</td></tr>@endforelse</tbody></table></div>{{ $users->links() }}</div></div></div></div>
@endsection
