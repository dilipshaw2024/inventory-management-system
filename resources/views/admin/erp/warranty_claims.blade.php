@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Warranty Claims</h4><p class="text-muted">Register and review warranty issues against service assets.</p></div>
<div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('erp.service.warranty.claims.store') }}">@csrf<div class="row g-2"><div class="col-md-3"><select name="asset_id" class="form-select" required><option value="">Asset</option>@foreach($assets as $asset)<option value="{{ $asset->id }}">{{ $asset->asset_no }} — {{ $asset->name }} @if($asset->warranty_until)({{ $asset->warranty_until->format('Y-m-d') }})@endif</option>@endforeach</select></div><div class="col-md-2"><input name="claim_no" class="form-control" placeholder="Claim no (optional)"></div><div class="col-md-2"><input name="received_at" type="date" value="{{ now()->toDateString() }}" class="form-control" required></div><div class="col-md-4"><input name="issue" class="form-control" placeholder="Issue description" required></div><div class="col-md-1"><button class="btn btn-primary">Submit</button></div></div></form></div></div>
<div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Claim</th><th>Asset</th><th>Customer</th><th>Received</th><th>Issue</th><th>Status</th><th>Coverage</th></tr></thead><tbody>@forelse($claims as $claim)<tr><td>{{ $claim->claim_no }}</td><td>{{ $claim->asset->asset_no }} — {{ $claim->asset->name }}</td><td>{{ $claim->customer->name ?? '—' }}</td><td>{{ optional($claim->received_at)->format('Y-m-d') }}</td><td>{{ $claim->issue }}</td><td>{{ ucfirst(str_replace('_', ' ', $claim->status)) }}</td><td>{{ is_null($claim->covered) ? 'Pending review' : ($claim->covered ? 'Covered' : 'Not covered') }}</td></tr>@empty<tr><td colspan="7" class="text-center">No warranty claims.</td></tr>@endforelse</tbody></table>{{ $claims->links() }}</div></div></div></div>
@endsection
