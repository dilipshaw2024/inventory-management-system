@extends('admin.admin_master')

@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Active Sessions</h4><p class="text-muted">Review signed-in devices and revoke sessions you no longer trust.</p></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Device / browser</th><th>IP address</th><th>Last activity</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($sessions as $session)<tr><td>{{ \Illuminate\Support\Str::limit($session->user_agent ?: 'Unknown device', 100) }} @if($session->session_hash === $currentHash)<span class="badge bg-primary">This device</span>@endif</td><td>{{ $session->ip_address ?: '—' }}</td><td>{{ optional($session->last_activity)->format('Y-m-d H:i:s') ?: '—' }}</td><td>{{ $session->revoked_at ? 'Revoked' : 'Active' }}</td><td>@if(!$session->revoked_at)<form method="POST" action="{{ route('security.sessions.revoke', $session->id) }}">@csrf<button class="btn btn-sm btn-outline-danger">Revoke</button></form>@else — @endif</td></tr>@empty<tr><td colspan="5" class="text-center">No session records.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
