@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Multi-factor Authentication</h4></div><div class="card"><div class="card-body"><p><strong>Account:</strong> {{ auth()->user()->email ?: auth()->user()->username }}</p>@if(auth()->user()->mfa_enabled)<div class="alert alert-success">MFA is enabled. Recovery codes are shown only once after enrollment.</div>@if($recoveryCodes)<div class="alert alert-warning"><strong>Save these recovery codes:</strong><div class="mt-2">@foreach($recoveryCodes as $code)<code class="me-3">{{ $code }}</code>@endforeach</div></div>@endif<form method="POST" action="{{ route('mfa.disable') }}" class="mt-3">@csrf<div class="input-group"><input name="password" type="password" class="form-control" placeholder="Current password" required><button class="btn btn-outline-danger">Disable MFA</button></div></form>@else<p class="text-muted">Add the secret to an authenticator app, then confirm the six-digit code.</p><label class="form-label">Manual setup key</label><input class="form-control mb-2" value="{{ $secret }}" readonly onclick="this.select()"><small class="text-muted">Provisioning URI:</small><textarea class="form-control mb-3" rows="2" readonly>{{ $uri }}</textarea><form method="POST" action="{{ route('mfa.enable') }}">@csrf<label class="form-label">Authenticator code</label><input name="code" class="form-control mb-3" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" required><button class="btn btn-primary">Enable MFA</button></form>@endif</div></div></div></div>
@endsection
