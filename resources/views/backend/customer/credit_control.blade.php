@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Customer Credit Control</h4><p class="text-muted">Set limits and overdue controls used during sales-order approval. A zero limit or overdue threshold disables that control.</p></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Customer</th><th>Outstanding</th><th>Overdue</th><th>Status</th><th>Credit limit</th><th>Terms (days)</th><th>Hold after (days)</th><th>Hold</th><th></th></tr></thead><tbody>@forelse($customers as $customer)<tr><form method="POST" action="{{ route('customer.credit.update', $customer->id) }}">@csrf @method('PUT')<td>{{ $customer->name }}</td><td>{{ number_format((float) ($customer->outstanding ?? 0), 2) }}</td><td>{{ number_format((float) ($customer->overdue_amount ?? 0), 2) }}<small class="d-block text-muted">{{ $customer->oldest_overdue_days ?? 0 }} days</small></td><td>{{ ucfirst(str_replace('_', ' ', $customer->collection_status ?? 'current')) }}</td><td><input name="credit_limit" type="number" min="0" step="0.01" value="{{ $customer->credit_limit }}" class="form-control"></td><td><input name="credit_days" type="number" min="0" max="3650" value="{{ $customer->credit_days }}" class="form-control"></td><td><input name="credit_hold_after_days" type="number" min="0" max="3650" value="{{ $customer->credit_hold_after_days ?? 0 }}" class="form-control"></td><td><input name="credit_hold" type="checkbox" value="1" @checked($customer->credit_hold)> Hold</td><td><button class="btn btn-sm btn-primary">Save</button></td></form></tr>@empty<tr><td colspan="9" class="text-center">No customers.</td></tr>@endforelse</tbody></table></div>{{ $customers->links() }}</div></div></div></div>
@endsection
