@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Customer Payment Allocations</h4><p class="text-muted">Allocate unallocated receipts and reverse posted receipts with an auditable reason.</p></div><div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('customer.payment.allocations.store') }}" class="row g-3">@csrf<div class="col-md-4"><label>Unallocated payment</label><select name="payment_id" id="allocation-payment" class="form-select" required><option value="">Select payment</option>@foreach($payments->where('is_reversed', false)->filter(fn ($payment) => !$payment->invoice_id) as $payment)<option value="{{ $payment->id }}" data-customer="{{ $payment->customer_id }}">{{ $payment->reference ?: ('Payment #'.$payment->id) }} — {{ $payment->customer->name }} — {{ number_format((float) $payment->paid_amount - (float) $payment->allocations->sum('amount'), 2) }} available</option>@endforeach</select></div><div class="col-md-4"><label>Approved invoice</label><select name="invoice_id" id="allocation-invoice" class="form-select" required><option value="">Select invoice</option>@foreach($invoices as $invoice)<option value="{{ $invoice->id }}" data-customer="{{ $invoice->customer_id }}">{{ $invoice->invoice_no }} — {{ $invoice->customer->name }} — {{ number_format((float) $invoice->total_amount, 2) }}</option>@endforeach</select></div><div class="col-md-2"><label>Amount</label><input name="amount" type="number" min="0.000001" step="0.000001" class="form-control" required></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Allocate</button></div></form></div></div><div class="card"><div class="card-body"><h5>Payment history</h5><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Reference</th><th>Customer</th><th>Amount</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->reference ?: ('Payment #'.$payment->id) }}</td><td>{{ $payment->customer->name }}</td><td>{{ number_format((float) $payment->paid_amount, 2) }}</td><td>@if($payment->is_reversed)<span class="badge bg-secondary">Reversed</span>@else<span class="badge bg-success">Posted</span>@endif</td><td>@if(!$payment->is_reversed)<form method="POST" action="{{ route('customer.payments.reverse', $payment->id) }}" class="d-flex gap-1">@csrf<input name="reason" class="form-control form-control-sm" placeholder="Reason" required><button class="btn btn-sm btn-outline-danger">Reverse</button></form>@else — @endif</td></tr>@empty<tr><td colspan="5" class="text-center">No customer payments.</td></tr>@endforelse</tbody></table></div></div></div></div></div><script>document.getElementById('allocation-payment').addEventListener('change',function(){const customer=this.selectedOptions[0]?.dataset.customer;document.querySelectorAll('#allocation-invoice option').forEach(o=>{o.hidden=!!o.value&&!!customer&&o.dataset.customer!==customer;});document.getElementById('allocation-invoice').value='';});</script>
@endsection
