@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-sm-flex align-items-center justify-content-between"><h4>Sales Orders</h4><a href="{{ route('fulfillment.orders.create') }}" class="btn btn-primary">New sales order</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Order no.</th><th>Customer</th><th>Date</th><th>Status</th><th>Promotion</th><th>Backorder</th><th>Action</th></tr></thead><tbody>@forelse($orders as $order)<tr><td>{{ $order->order_no }}</td><td>{{ data_get($order, 'customer.name', 'N/A') }}</td><td>{{ optional($order->date)->format('d-m-Y') }}</td><td>{{ ucwords(str_replace('_', ' ', $order->status)) }} @if($order->status === 'rejected')<small class="d-block text-muted">{{ $order->rejection_reason }}</small>@endif</td><td>{{ $order->promotion?->code ?? '—' }}</td><td>{{ $order->allow_backorders ? 'Allowed' : 'Strict' }}</td><td>@if($order->status === 'submitted')<form method="POST" action="{{ route('fulfillment.orders.approve', $order->id) }}" class="d-inline">@csrf<button class="btn btn-success btn-sm">Approve</button></form><form method="POST" action="{{ route('fulfillment.orders.reject', $order->id) }}" class="d-inline ms-1">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-outline-warning btn-sm">Reject</button></form>@endif @if(in_array($order->status, ['submitted', 'approved', 'partially_delivered'], true))<form method="POST" action="{{ route('fulfillment.orders.cancel', $order->id) }}" class="d-inline ms-1">@csrf<input name="cancellation_reason" value="Cancelled by operator" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-outline-danger btn-sm">Cancel</button></form>@endif @if(!in_array($order->status, ['submitted', 'approved', 'partially_delivered'], true))—@endif</td></tr>@empty<tr><td colspan="7" class="text-center">No sales orders found.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div></div></div></div>
@endsection
