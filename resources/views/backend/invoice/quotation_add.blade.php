@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>New Sales Quotation</h4></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('sales.quotations.store') }}">@csrf<div class="row g-3 mb-3"><div class="col-md-3"><label>Quote no.</label><input name="quote_no" value="{{ old('quote_no', 'QT-'.now()->format('YmdHis')) }}" class="form-control"></div><div class="col-md-3"><label>Customer</label><select name="customer_id" class="form-select" required><option value="">Select customer</option>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }}</option>@endforeach</select></div><div class="col-md-3"><label>Quote date</label><input type="date" name="quote_date" value="{{ old('quote_date', now()->toDateString()) }}" class="form-control" required></div><div class="col-md-3"><label>Valid until</label><input type="date" name="valid_until" class="form-control"></div></div><textarea name="description" class="form-control mb-3" placeholder="Terms and description">{{ old('description') }}</textarea><div class="table-responsive"><table class="table" id="quote-lines"><thead><tr><th>Product</th><th>Quantity</th><th>Unit price</th><th>Discount</th><th></th></tr></thead><tbody><tr><td><select name="product_id[]" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></td><td><input name="quantity[]" type="number" min="0.000001" step="0.000001" class="form-control" required></td><td><input name="unit_price[]" type="number" min="0" step="0.000001" class="form-control" required></td><td><input name="discount_amount[]" type="number" min="0" step="0.000001" value="0" class="form-control" required></td><td><button type="button" class="btn btn-outline-danger remove-line">Remove</button></td></tr></tbody></table></div><button type="button" id="add-line" class="btn btn-light">Add line</button> <button class="btn btn-primary">Submit quotation</button></form></div></div></div></div><script>document.getElementById('add-line').addEventListener('click',function(){const row=document.querySelector('#quote-lines tbody tr').cloneNode(true);row.querySelectorAll('input').forEach(i=>i.value=i.name.startsWith('discount')?'0':'');row.querySelectorAll('select').forEach(s=>s.selectedIndex=0);document.querySelector('#quote-lines tbody').appendChild(row);});document.addEventListener('click',function(e){if(e.target.classList.contains('remove-line')&&document.querySelectorAll('#quote-lines tbody tr').length>1)e.target.closest('tr').remove();});</script>
@endsection
