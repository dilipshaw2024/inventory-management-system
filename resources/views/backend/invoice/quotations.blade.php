@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-sm-flex align-items-center justify-content-between"><h4>Sales Quotations</h4><a href="{{ route('sales.quotations.create') }}" class="btn btn-primary">New quotation</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>No.</th><th>Customer</th><th>Date</th><th>Valid until</th><th>Lines</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($quotations as $quotation)<tr><td>{{ $quotation->quote_no }}</td><td>{{ data_get($quotation, 'customer.name', 'N/A') }}</td><td>{{ optional($quotation->quote_date)->format('d-m-Y') }}</td><td>{{ optional($quotation->valid_until)->format('d-m-Y') ?: '—' }}</td><td>{{ $quotation->lines->count() }}</td><td>{{ ucwords(str_replace('_', ' ', $quotation->status)) }} @if($quotation->status === 'rejected')<small class="d-block text-muted">{{ $quotation->rejection_reason }}</small>@endif</td><td class="d-flex gap-1">@if($quotation->status === 'submitted')<form method="POST" action="{{ route('sales.quotations.approve', $quotation->id) }}" class="d-inline">@csrf<button class="btn btn-success btn-sm">Approve</button></form><form method="POST" action="{{ route('sales.quotations.reject', $quotation->id) }}" class="d-inline">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-outline-warning btn-sm">Reject</button></form>@elseif($quotation->status === 'approved')<form method="POST" action="{{ route('sales.quotations.convert', $quotation->id) }}">@csrf<button class="btn btn-primary btn-sm">Convert to SO</button></form>@else—@endif</td></tr>@empty<tr><td colspan="7" class="text-center">No sales quotations found.</td></tr>@endforelse</tbody></table></div>{{ $quotations->links() }}</div></div></div></div>
@endsection
