@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Product Barcodes & QR Codes</h4><p class="text-muted">Maintain multiple scan codes per product; the primary code mirrors the legacy product barcode.</p></div>
<div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('product.barcodes.store') }}" class="row g-2">@csrf<div class="col-md-4"><select name="product_id" class="form-select" required><option value="">Product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }} ({{ $product->sku }})</option>@endforeach</select></div><div class="col-md-3"><input name="code" class="form-control" placeholder="Barcode / QR code" required></div><div class="col-md-2"><select name="type" class="form-select"><option value="barcode">Barcode</option><option value="qrcode">QR code</option></select></div><div class="col-md-2 form-check mt-2"><input name="is_primary" value="1" type="checkbox" class="form-check-input"> Primary</div><div class="col-md-1"><button class="btn btn-primary">Save</button></div></form></div></div>
<div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Code</th><th>Type</th><th>Primary</th><th>Created</th><th>Print</th></tr></thead><tbody>@forelse($barcodes as $barcode)<tr><td>{{ $barcode->product->name }}</td><td>{{ $barcode->code }}</td><td>{{ strtoupper($barcode->type) }}</td><td>{{ $barcode->is_primary ? 'Yes' : 'No' }}</td><td>{{ $barcode->created_at }}</td><td><a class="btn btn-sm btn-outline-dark" target="_blank" href="{{ route('product.barcodes.print', ['product_id' => $barcode->product_id]) }}">Print label</a></td></tr>@empty<tr><td colspan="6" class="text-center">No normalized barcodes.</td></tr>@endforelse</tbody></table>{{ $barcodes->links() }}</div></div></div></div>
@endsection
