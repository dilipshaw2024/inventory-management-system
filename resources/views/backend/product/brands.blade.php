@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Brand Management</h4><p class="text-muted">Maintain company-scoped product brands and codes.</p></div><div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('product.brands.store') }}" class="row g-2">@csrf<div class="col-md-4"><input name="name" class="form-control" placeholder="Brand name" required></div><div class="col-md-3"><input name="code" class="form-control" placeholder="Brand code" required></div><div class="col-md-2 form-check mt-2"><input name="is_active" value="1" type="checkbox" class="form-check-input" checked> Active</div><div class="col-md-2"><button class="btn btn-primary">Add brand</button></div></form></div></div><div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Name</th><th>Code</th><th>Products</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($brands as $brand)<tr><td>{{ $brand->name }}</td><td>{{ $brand->code }}</td><td>{{ $brand->products_count }}</td><td>{{ $brand->is_active ? 'Active' : 'Inactive' }}</td><td><form method="POST" action="{{ route('product.brands.update', $brand->id) }}" class="d-inline">@csrf @method('PUT')<input type="hidden" name="name" value="{{ $brand->name }}"><input type="hidden" name="code" value="{{ $brand->code }}"><input type="hidden" name="is_active" value="{{ $brand->is_active ? 1 : 0 }}"><button class="btn btn-sm btn-outline-secondary">Save</button></form><form method="POST" action="{{ route('product.brands.destroy', $brand->id) }}" class="d-inline" onsubmit="return confirm('Delete this brand?');">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="text-center">No brands found.</td></tr>@endforelse</tbody></table>{{ $brands->links() }}</div></div></div></div>
@endsection
