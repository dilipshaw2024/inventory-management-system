@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Inventory Costing Methods</h4><p class="text-muted">FIFO is the default. Weighted average and moving average revalue open layers; standard cost uses the configured standard cost for issues.</p></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Method</th><th>Standard cost</th><th>Save</th></tr></thead><tbody>@forelse($products as $product)<tr><form method="POST" action="{{ route('product.costing.update', $product->id) }}">@csrf @method('PUT')<td>{{ $product->name }}</td><td><select name="costing_method" class="form-select"><option value="fifo" @selected($product->costing_method === 'fifo')>FIFO</option><option value="weighted_average" @selected($product->costing_method === 'weighted_average')>Weighted average</option><option value="moving_average" @selected($product->costing_method === 'moving_average')>Moving average</option><option value="standard" @selected($product->costing_method === 'standard')>Standard cost</option></select></td><td><input name="standard_cost" type="number" min="0" step="0.000001" value="{{ $product->standard_cost }}" class="form-control"></td><td><button class="btn btn-sm btn-primary">Save</button></td></form></tr>@empty<tr><td colspan="4" class="text-center">No products.</td></tr>@endforelse</tbody></table></div>{{ $products->links() }}</div></div></div></div>
@endsection
