@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Product Import Validation</h4><p class="text-muted">Dry-run preview only. No catalog records were changed.</p></div>@if($errors)<div class="alert alert-danger"><strong>{{ count($errors) }} validation issue(s)</strong><a href="{{ route('product.import.errors') }}" class="btn btn-sm btn-outline-danger float-end">Download errors</a><ul class="mb-0">@foreach($errors as $error)<li>{{ $error }}</li>@endforeach</ul></div>@else<div class="alert alert-success">All {{ count($rows) }} row(s) passed validation and are ready to import.</div>@endif<div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Row</th><th>Name</th><th>SKU</th><th>Barcode</th><th>Tracking</th><th>Action</th></tr></thead><tbody>@foreach($rows as $index => $row)<tr><td>{{ $index + 2 }}</td><td>{{ $row['name'] ?? '—' }}</td><td>{{ $row['sku'] ?? '—' }}</td><td>{{ $row['barcode'] ?? '—' }}</td><td>{{ $row['tracking_type'] ?? 'none' }}</td><td>{{ collect($errors)->contains(fn ($error) => str_starts_with($error, 'Row '.($index + 2).':')) ? 'Review' : 'Valid' }}</td></tr>@endforeach</tbody></table><a href="{{ route('product.all') }}" class="btn btn-light">Back to products</a></div></div></div></div>
@endsection
