@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Product Units of Measure</h4><p class="text-muted">Configure how purchase or sales units convert into the product stock unit. Example: 1 carton = 24 pieces.</p></div><div class="card mb-3"><div class="card-body"><form method="POST" action="{{ route('product.uoms.store') }}">@csrf<div class="row g-2"><div class="col-md-3"><select name="product_id" class="form-select" required><option value="">Product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }} (stock: {{ $product->unit->name }})</option>@endforeach</select></div><div class="col-md-2"><select name="unit_id" class="form-select" required><option value="">Transaction unit</option>@foreach($units as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select></div><div class="col-md-2"><input name="conversion_to_stock" type="number" min="0.00000001" step="0.00000001" class="form-control" placeholder="× stock qty" required></div><div class="col-md-2"><select name="usage" class="form-select"><option value="both">Purchase & sales</option><option value="purchase">Purchase only</option><option value="sales">Sales only</option></select></div><div class="col-md-1"><input name="decimal_places" type="number" min="0" max="8" value="3" class="form-control" title="Decimal places"></div><div class="col-md-2"><button class="btn btn-primary w-100">Save conversion</button></div></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Unit</th><th>Conversion</th><th>Usage</th><th>Status</th><th></th></tr></thead><tbody>@forelse($uoms as $uom)<tr><td>{{ $uom->product->name }}</td><td>{{ $uom->unit->name }}</td><td>1 {{ $uom->unit->name }} = {{ $uom->conversion_to_stock }} stock units</td><td>{{ ucfirst($uom->usage) }}</td><td>{{ $uom->is_active ? 'Active' : 'Inactive' }}</td><td>@if($uom->is_active)<form method="POST" action="{{ route('product.uoms.destroy', $uom->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Deactivate</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center">No product UOM conversions configured.</td></tr>@endforelse</tbody></table></div>{{ $uoms->links() }}</div></div></div></div>
@endsection
