@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><div><h4>Supplier Purchase Invoices</h4><p class="text-muted">Three-way matching against purchase orders and goods receipts.</p></div><a href="{{ route('procurement.invoices.create') }}" class="btn btn-primary">New invoice</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Invoice</th><th>Supplier</th><th>Total</th><th>Date</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td>{{ $invoice->invoice_no }}</td><td>{{ $invoice->supplier->name }}</td><td>{{ number_format((float) $invoice->total_amount, 2) }}</td><td>{{ optional($invoice->invoice_date)->format('Y-m-d') }}</td><td>{{ ucfirst($invoice->status) }} @if($invoice->status === 'rejected')<small class="d-block text-muted">{{ $invoice->rejection_reason }}</small>@endif</td><td>@if($invoice->status === 'pending')<form method="POST" action="{{ route('procurement.invoices.approve', $invoice->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success">Match &amp; approve</button></form><form method="POST" action="{{ route('procurement.invoices.reject', $invoice->id) }}" class="d-inline ms-1">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-sm btn-outline-warning">Reject</button></form>@else — @endif</td></tr>@empty<tr><td colspan="6" class="text-center">No supplier invoices.</td></tr>@endforelse</tbody></table></div>{{ $invoices->links() }}</div></div></div></div>
@endsection
