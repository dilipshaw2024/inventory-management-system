@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><div><h4>Landed Costs</h4><p class="text-muted">Approved charges are allocated into remaining inventory cost layers.</p></div><a href="{{ route('procurement.landed.costs.create') }}" class="btn btn-primary">Add landed cost</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Cost no.</th><th>Receipt</th><th>Type</th><th>Amount</th><th>Allocation</th><th>Status</th><th></th></tr></thead><tbody>@forelse($costs as $cost)<tr><td>{{ $cost->cost_no }}</td><td>{{ $cost->goodsReceipt->grn_no }}</td><td>{{ ucfirst($cost->cost_type) }}</td><td>{{ number_format((float) $cost->amount, 2) }}</td><td>{{ str_replace('_', ' ', $cost->allocation_method) }}</td><td>{{ ucfirst($cost->status) }} @if($cost->status === 'rejected')<small class="d-block text-muted">{{ $cost->rejection_reason }}</small>@endif</td><td>@if($cost->status === 'pending')<form method="POST" action="{{ route('procurement.landed.costs.approve', $cost->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success">Approve &amp; allocate</button></form><form method="POST" action="{{ route('procurement.landed.costs.reject', $cost->id) }}" class="d-inline ms-1">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-sm btn-outline-warning">Reject</button></form>@else — @endif</td></tr>@empty<tr><td colspan="7" class="text-center">No landed costs.</td></tr>@endforelse</tbody></table></div>{{ $costs->links() }}</div></div></div></div>
@endsection
