@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>New Purchase Order</h4><p class="text-muted">Use a configured transaction unit; quantities are normalized to the product stock unit.</p></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('procurement.orders.store') }}">@csrf<div class="row g-3 mb-3"><div class="col-md-3"><label class="form-label">PO no.</label><input name="po_no" value="{{ old('po_no', 'PO-'.now()->format('YmdHis')) }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Supplier</label><select name="supplier_id" class="form-select" required><option value="">Select supplier</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected(old('supplier_id', $selectedSupplierId) == $supplier->id)>{{ $supplier->name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Date</label><input type="date" name="date" value="{{ old('date', now()->toDateString()) }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Expected date</label><input type="date" name="expected_date" class="form-control"></div></div><div class="mb-3"><textarea name="description" class="form-control" placeholder="Description"></textarea></div><div class="table-responsive"><table class="table" id="po-lines"><thead><tr><th>Product</th><th>UOM</th><th>Quantity</th><th>Unit price</th><th></th></tr></thead><tbody><tr><td><select name="product_id[]" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(old('product_id.0', $selectedProductId) == $product->id)>{{ $product->name }}</option>@endforeach</select></td><td><select name="uom_id[]" class="form-select"><option value="">Stock unit</option>@foreach($units as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select></td><td><input name="ordered_qty[]" value="{{ old('ordered_qty.0', $suggestedQuantity) }}" type="number" min="0.000001" step="0.000001" class="form-control" required></td><td><input name="unit_price[]" value="{{ old('unit_price.0', $suggestedPrice) }}" type="number" min="0" step="0.000001" class="form-control" required></td><td><button type="button" class="btn btn-outline-danger remove-line">Remove</button></td></tr></tbody></table></div><button type="button" id="add-line" class="btn btn-light">Add line</button> <button class="btn btn-primary">Submit order</button></form></div></div></div></div><script>document.getElementById('add-line').addEventListener('click',function(){const row=document.querySelector('#po-lines tbody tr').cloneNode(true);row.querySelectorAll('input').forEach(i=>i.value='');row.querySelectorAll('select').forEach(s=>s.selectedIndex=0);document.querySelector('#po-lines tbody').appendChild(row);});document.addEventListener('click',function(e){if(e.target.classList.contains('remove-line')&&document.querySelectorAll('#po-lines tbody tr').length>1)e.target.closest('tr').remove();});</script>
@endsection
