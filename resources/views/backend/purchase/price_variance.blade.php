@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Purchase Price Variance</h4><p class="text-muted">Invoice prices compared with purchase-order prices. Configured blocking tolerance: {{ number_format($tolerance, 2) }}%.</p></div><div class="card mb-3"><div class="card-body"><form method="GET" class="row g-2"><div class="col-md-3"><label>Minimum absolute variance (%)</label><input name="minimum_percent" type="number" min="0" max="1000" step="0.01" value="{{ $minimum }}" class="form-control"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary">Filter</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Invoice</th><th>Supplier</th><th>Product</th><th>PO price</th><th>Invoice price</th><th>Quantity</th><th>Variance %</th><th>Variance value</th><th>Status</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row['invoice']->invoice_no }}</td><td>{{ $row['invoice']->supplier->name }}</td><td>{{ $row['line']->product->name }}</td><td>{{ number_format($row['ordered_price'], 4) }}</td><td>{{ number_format((float) $row['line']->unit_price, 4) }}</td><td>{{ number_format((float) $row['line']->quantity, 3) }}</td><td class="{{ abs($row['variance_percent']) > $tolerance && $tolerance > 0 ? 'text-danger fw-bold' : '' }}">{{ number_format($row['variance_percent'], 2) }}%</td><td>{{ number_format($row['variance_amount'], 4) }}</td><td>@if($tolerance > 0 && abs($row['variance_percent']) > $tolerance)<span class="badge bg-danger">Over tolerance</span>@else<span class="badge bg-success">Within tolerance</span>@endif</td></tr>@empty<tr><td colspan="9" class="text-center">No price variances found.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
