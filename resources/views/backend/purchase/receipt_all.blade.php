@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-sm-flex align-items-center justify-content-between"><h4>Goods Receipts</h4><a href="{{ route('procurement.receipts.create') }}" class="btn btn-primary">New goods receipt</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>GRN no.</th><th>Purchase order</th><th>Supplier</th><th>Date</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($receipts as $receipt)<tr><td>{{ $receipt->grn_no }}</td><td>{{ data_get($receipt, 'purchaseOrder.po_no', 'N/A') }}</td><td>{{ data_get($receipt, 'purchaseOrder.supplier.name', 'N/A') }}</td><td>{{ optional($receipt->date)->format('d-m-Y') }}</td><td>{{ ucfirst($receipt->status) }} @if($receipt->status === 'rejected')<small class="d-block text-muted">{{ $receipt->rejection_reason }}</small>@endif</td><td>@if($receipt->status === 'pending')<form method="POST" action="{{ route('procurement.receipts.approve', $receipt->id) }}" class="d-inline">@csrf<button class="btn btn-success btn-sm">Approve and receive</button></form><form method="POST" action="{{ route('procurement.receipts.reject', $receipt->id) }}" class="d-inline ms-1">@csrf<input name="rejection_reason" value="Rejected by receiver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-outline-warning btn-sm">Reject</button></form>@else—@endif</td></tr>@empty<tr><td colspan="6" class="text-center">No goods receipts found.</td></tr>@endforelse</tbody></table></div>{{ $receipts->links() }}</div></div></div></div>
<div class="card mt-3"><div class="card-body"><h5>Pending quality inspections</h5><div class="table-responsive"><table class="table table-sm"><thead><tr><th>GRN</th><th>Inspection</th><th>Notes</th><th>Action</th></tr></thead><tbody>@forelse($receipts->filter(fn ($receipt) => $receipt->status === 'pending' && ($receipt->inspection_status ?? 'not_required') === 'pending') as $receipt)<tr><td>{{ $receipt->grn_no }}</td><td><form method="POST" action="{{ route('procurement.receipts.inspect', $receipt->id) }}" class="d-flex gap-1">@csrf<select name="inspection_status" class="form-select form-select-sm" required><option value="passed">Pass</option><option value="failed">Fail</option></select><input name="inspection_notes" class="form-control form-control-sm" placeholder="Inspection notes" required maxlength="3000"><button class="btn btn-sm btn-outline-primary">Record</button></form></td><td>{{ $receipt->inspection_notes ?: 'Awaiting inspection' }}</td><td>Stock posting is blocked until passed.</td></tr>@empty<tr><td colspan="4" class="text-center">No receipts are awaiting inspection.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
