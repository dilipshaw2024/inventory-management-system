@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>New Purchase Requisition</h4></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('procurement.requisitions.store') }}">@csrf<div class="row g-3 mb-3"><div class="col-md-3"><label>Requisition no.</label><input name="requisition_no" value="{{ old('requisition_no', 'PR-'.now()->format('YmdHis')) }}" class="form-control"></div><div class="col-md-3"><label>Requested date</label><input type="date" name="requested_date" value="{{ old('requested_date', now()->toDateString()) }}" class="form-control" required></div><div class="col-md-3"><label>Required date</label><input type="date" name="required_date" class="form-control"></div><div class="col-md-3"><label>Suggested supplier</label><select name="suggested_supplier_id" class="form-select"><option value="">Select supplier</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}">{{ $supplier->name }}</option>@endforeach</select></div></div><textarea name="description" class="form-control mb-3" placeholder="Business need / justification">{{ old('description') }}</textarea><div class="table-responsive"><table class="table" id="requisition-lines"><thead><tr><th>Product</th><th>Quantity</th><th>Estimated price</th><th>Notes</th><th></th></tr></thead><tbody><tr><td><select name="product_id[]" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></td><td><input name="requested_qty[]" type="number" min="0.000001" step="0.000001" class="form-control" required></td><td><input name="estimated_unit_price[]" type="number" min="0" step="0.000001" class="form-control" value="0" required></td><td><input name="notes[]" class="form-control"></td><td><button type="button" class="btn btn-outline-danger remove-line">Remove</button></td></tr></tbody></table></div><button type="button" id="add-line" class="btn btn-light">Add line</button> <button class="btn btn-primary">Submit requisition</button></form></div></div></div></div><script>document.getElementById('add-line').addEventListener('click',function(){const row=document.querySelector('#requisition-lines tbody tr').cloneNode(true);row.querySelectorAll('input').forEach(i=>i.value=i.name.startsWith('estimated')?'0':'');row.querySelectorAll('select').forEach(s=>s.selectedIndex=0);document.querySelector('#requisition-lines tbody').appendChild(row);});document.addEventListener('click',function(e){if(e.target.classList.contains('remove-line')&&document.querySelectorAll('#requisition-lines tbody tr').length>1)e.target.closest('tr').remove();});</script>
@endsection
