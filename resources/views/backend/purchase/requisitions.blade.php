@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-sm-flex align-items-center justify-content-between"><h4>Purchase Requisitions</h4><a href="{{ route('procurement.requisitions.create') }}" class="btn btn-primary">New requisition</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>No.</th><th>Date</th><th>Supplier</th><th>Lines</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($requisitions as $requisition)<tr><td>{{ $requisition->requisition_no }}</td><td>{{ optional($requisition->requested_date)->format('d-m-Y') }}</td><td>{{ data_get($requisition, 'supplier.name', 'Not specified') }}</td><td>{{ $requisition->lines->count() }}</td><td>{{ ucwords(str_replace('_', ' ', $requisition->status)) }} @if($requisition->status === 'rejected')<small class="d-block text-muted">{{ $requisition->rejection_reason }}</small>@endif</td><td class="d-flex gap-1">@if($requisition->status === 'submitted')<form method="POST" action="{{ route('procurement.requisitions.approve', $requisition->id) }}" class="d-inline">@csrf<button class="btn btn-success btn-sm">Approve</button></form><form method="POST" action="{{ route('procurement.requisitions.reject', $requisition->id) }}" class="d-inline">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-outline-warning btn-sm">Reject</button></form>@elseif($requisition->status === 'approved')<form method="POST" action="{{ route('procurement.requisitions.convert', $requisition->id) }}">@csrf<button class="btn btn-primary btn-sm">Convert to PO</button></form>@else—@endif</td></tr>@empty<tr><td colspan="6" class="text-center">No purchase requisitions found.</td></tr>@endforelse</tbody></table></div>{{ $requisitions->links() }}</div></div></div></div>
@endsection
