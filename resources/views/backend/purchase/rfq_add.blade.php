@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Issue Purchase RFQ</h4></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('procurement.rfqs.store') }}">@csrf<div class="row g-3 mb-3"><div class="col-md-3"><label>RFQ number</label><input name="rfq_no" class="form-control" value="{{ old('rfq_no') }}"></div><div class="col-md-3"><label>Issue date</label><input type="date" name="issue_date" required class="form-control" value="{{ old('issue_date', now()->toDateString()) }}"></div><div class="col-md-3"><label>Response due</label><input type="date" name="response_due" class="form-control" value="{{ old('response_due') }}"></div></div><textarea name="description" class="form-control mb-3" placeholder="Requirements and commercial terms">{{ old('description') }}</textarea><label>Suppliers</label><select name="supplier_id[]" multiple required class="form-select mb-3" size="5">@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}">{{ $supplier->name }}</option>@endforeach</select><div class="table-responsive"><table class="table" id="rfq-lines"><thead><tr><th>Product</th><th>Quantity</th><th>Notes</th><th></th></tr></thead><tbody><tr><td><select name="product_id[]" required class="form-select"><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></td><td><input name="requested_qty[]" required type="number" min="0.000001" step="0.000001" class="form-control"></td><td><input name="notes[]" class="form-control"></td><td><button type="button" class="btn btn-outline-danger remove-line">Remove</button></td></tr></tbody></table></div><button type="button" id="add-line" class="btn btn-light">Add line</button> <button class="btn btn-primary">Issue RFQ</button></form></div></div></div></div><script>document.getElementById('add-line').addEventListener('click',function(){const r=document.querySelector('#rfq-lines tbody tr').cloneNode(true);r.querySelectorAll('input').forEach(i=>i.value='');r.querySelector('select').selectedIndex=0;document.querySelector('#rfq-lines tbody').appendChild(r);});document.addEventListener('click',function(e){if(e.target.classList.contains('remove-line')&&document.querySelectorAll('#rfq-lines tbody tr').length>1)e.target.closest('tr').remove();});</script>
@endsection
