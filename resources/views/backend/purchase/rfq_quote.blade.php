@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Supplier Quotation: {{ $supplier->supplier->name }}</h4><p class="text-muted">{{ $rfq->rfq_no }} — enter the supplier's response for each requested line.</p></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('procurement.rfqs.quote', $rfq->id) }}">@csrf<input type="hidden" name="rfq_supplier_id" value="{{ $supplier->id }}"><div class="table-responsive"><table class="table"><thead><tr><th>Product</th><th>Requested</th><th>Unit price</th><th>Lead days</th></tr></thead><tbody>@foreach($rfq->lines as $line)<tr><td>{{ $line->product->name }}<input type="hidden" name="line_id[]" value="{{ $line->id }}"></td><td>{{ $line->requested_qty }}</td><td><input required type="number" min="0" step="0.000001" name="unit_price[]" class="form-control" value="{{ optional($supplier->quotations->firstWhere('purchase_rfq_line_id', $line->id))->unit_price }}"></td><td><input type="number" min="0" name="lead_days[]" class="form-control" value="{{ optional($supplier->quotations->firstWhere('purchase_rfq_line_id', $line->id))->lead_days }}"></td></tr>@endforeach</tbody></table></div><div class="row g-3"><div class="col-md-4"><label>Valid until</label><input type="date" name="valid_until" class="form-control"></div><div class="col-md-4"><label>Supplier reference</label><input name="supplier_reference" class="form-control"></div><div class="col-md-4"><label>Notes</label><input name="notes" class="form-control"></div></div><button class="btn btn-primary mt-3">Save quotation</button></form></div></div></div></div>
@endsection
