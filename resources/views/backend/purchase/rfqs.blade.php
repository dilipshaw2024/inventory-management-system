@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><h4>Purchase RFQs</h4><a href="{{ route('procurement.rfqs.create') }}" class="btn btn-primary">New RFQ</a></div><div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>RFQ</th><th>Issue date</th><th>Response due</th><th>Lines</th><th>Suppliers / quotes</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($rfqs as $rfq)<tr><td>{{ $rfq->rfq_no }}</td><td>{{ optional($rfq->issue_date)->format('d-m-Y') }}</td><td>{{ optional($rfq->response_due)->format('d-m-Y') ?: '—' }}</td><td>{{ $rfq->lines->count() }}</td><td>@foreach($rfq->suppliers as $response)<div>{{ $response->supplier->name }} <span class="badge bg-{{ $response->status === 'quoted' ? 'success' : 'secondary' }}">{{ $response->status }} ({{ $response->quotations->count() }}/{{ $rfq->lines->count() }})</span> <a class="btn btn-sm btn-outline-primary" href="{{ route('procurement.rfqs.quote.create', [$rfq->id, $response->id]) }}">{{ $response->status === 'quoted' ? 'Edit quote' : 'Enter quote' }}</a></div>@endforeach</td><td>{{ ucfirst($rfq->status) }} @if($rfq->status === 'rejected')<small class="d-block text-muted">{{ $rfq->rejection_reason }}</small>@endif</td><td><a class="btn btn-sm btn-outline-info mb-1" href="{{ route('procurement.rfqs.compare', $rfq->id) }}">Compare</a>@if($rfq->status === 'submitted')<form method="POST" action="{{ route('procurement.rfqs.close', $rfq->id) }}" class="d-inline">@csrf<button class="btn btn-outline-secondary btn-sm">Close</button></form><form method="POST" action="{{ route('procurement.rfqs.reject', $rfq->id) }}" class="d-inline">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:145px" required maxlength="2000"><button class="btn btn-outline-warning btn-sm">Reject</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="text-center">No RFQs found.</td></tr>@endforelse</tbody></table>{{ $rfqs->links() }}</div></div></div></div>
@endsection
