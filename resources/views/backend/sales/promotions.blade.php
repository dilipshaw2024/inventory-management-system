@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><h4>Sales Promotions</h4><a href="{{ route('sales.promotions.create') }}" class="btn btn-primary">New promotion</a></div><div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Code</th><th>Name</th><th>Type</th><th>Value</th><th>Eligibility</th><th>Validity</th><th>Usage</th><th>Status</th></tr></thead><tbody>@forelse($promotions as $promotion)<tr><td>{{ $promotion->code }}</td><td>{{ $promotion->name }}</td><td>{{ ucfirst($promotion->type) }}</td><td>{{ $promotion->discount_value }}</td><td>{{ data_get($promotion, 'product.name', data_get($promotion, 'category.name', data_get($promotion, 'customer.name', 'All eligible'))) }}</td><td>{{ optional($promotion->starts_on)->format('d-m-Y') ?: 'Any' }} — {{ optional($promotion->ends_on)->format('d-m-Y') ?: 'Open' }}</td><td>{{ $promotion->usage_count }} / {{ $promotion->usage_limit ?: '∞' }}</td><td>{{ $promotion->is_active ? 'Active' : 'Inactive' }}</td></tr>@empty<tr><td colspan="8" class="text-center">No promotions configured.</td></tr>@endforelse</tbody></table>{{ $promotions->links() }}</div></div></div></div>
@endsection
