@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>{{ $opening ? 'Opening Stock' : 'New Inventory Adjustment' }}</h4><p class="text-muted">{{ $opening ? 'Load initial inventory balances by location, cost, batch, lot, and serial with approval.' : 'Submit a controlled stock increase or decrease for approval.' }}</p></div><div class="card"><div class="card-body"><form method="POST" action="{{ route($opening ? 'inventory.opening.store' : 'inventory.adjustments.store') }}">@csrf<div class="row g-3 mb-3"><div class="col-md-3"><label class="form-label">{{ $opening ? 'Opening document no.' : 'Adjustment no.' }}</label><input name="adjustment_no" value="{{ old('adjustment_no', ($opening ? 'OPEN-' : 'ADJ-').now()->format('YmdHis')) }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Date</label><input type="date" name="date" value="{{ old('date', now()->toDateString()) }}" class="form-control" required></div><div class="col-md-6">@if($opening)<label class="form-label">Reason code</label><input type="hidden" name="reason_code" value="opening_stock"><input value="Opening stock" class="form-control" readonly>@else<label class="form-label">Reason code</label><input name="reason_code" value="{{ old('reason_code') }}" placeholder="Damage, count variance..." class="form-control" required>@endif</div></div><div class="mb-3"><label class="form-label">Description</label><textarea name="description" class="form-control">{{ old('description') }}</textarea></div><div class="table-responsive"><table class="table" id="adjustment-lines"><thead><tr><th>Product</th>@unless($opening)<th>Direction</th>@endunless<th>Quantity</th><th>Unit cost</th><th>Location</th><th>Batch/Lot</th><th>Serial numbers</th><th>Expiry</th><th></th></tr></thead><tbody><tr><td><select name="product_id[]" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}{{ $product->sku ? ' ('.$product->sku.')' : '' }}</option>@endforeach</select></td>@if($opening)<input type="hidden" name="direction[]" value="in">@else<td><select name="direction[]" class="form-select"><option value="in">Increase</option><option value="out">Decrease</option></select></td>@endif<td><input name="quantity[]" type="number" step="0.000001" min="0.000001" class="form-control" required></td><td><input name="unit_cost[]" type="number" step="0.000001" min="0" class="form-control"></td><td><select name="location_id[]" class="form-select"><option value="">All locations</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->code }} — {{ $location->name }}</option>@endforeach</select></td><td><input name="batch_no[]" class="form-control" placeholder="Batch/lot"></td><td><input name="serial_numbers[]" class="form-control" placeholder="Comma separated"></td><td><input name="expiry_date[]" type="date" class="form-control"></td><input name="manufacturing_date[]" type="hidden"><input name="best_before_date[]" type="hidden"><input name="warranty_until[]" type="hidden"><td><button type="button" class="btn btn-outline-danger remove-line">Remove</button></td></tr></tbody></table></div><button type="button" id="add-line" class="btn btn-light">Add line</button> <button class="btn btn-primary">Submit for approval</button></form></div></div></div></div>
<script>document.getElementById('add-line').addEventListener('click', function () { const row = document.querySelector('#adjustment-lines tbody tr').cloneNode(true); row.querySelectorAll('input').forEach(input => input.value = ''); row.querySelectorAll('select').forEach(select => select.selectedIndex = 0); document.querySelector('#adjustment-lines tbody').appendChild(row); }); document.addEventListener('click', function (event) { if (event.target.classList.contains('remove-line') && document.querySelectorAll('#adjustment-lines tbody tr').length > 1) event.target.closest('tr').remove(); });</script>
@endsection
