@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Physical Stock Count</h4><p class="text-muted">Capture counted quantities, then submit the variance for approval.</p></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('inventory.counts.store') }}">@csrf<div class="row g-3"><div class="col-md-3"><label>Count number</label><input name="count_no" class="form-control" value="CNT-{{ now()->format('YmdHis') }}" required></div><div class="col-md-3"><label>Count date</label><input name="count_date" type="date" class="form-control" value="{{ now()->toDateString() }}" required></div><div class="col-md-4"><label>Location (optional)</label><select name="location_id" class="form-select"><option value="">All product balance</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->code }}</option>@endforeach</select></div><div class="col-md-2"><label>Description</label><input name="description" class="form-control"></div></div><div id="count-lines" class="mt-3"><div class="row g-2 count-line"><div class="col-md-6"><select name="product_id[]" class="form-select" required><option value="">Product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }} (system: {{ $product->quantity }})</option>@endforeach</select></div><div class="col-md-4"><input name="counted_quantity[]" type="number" min="0" step="0.000001" class="form-control" placeholder="Counted quantity" required></div></div></div><button type="button" class="btn btn-sm btn-light mt-2" onclick="addCountLine()">+ Add product</button><div class="mt-3"><button class="btn btn-primary">Submit count</button><a href="{{ route('inventory.counts') }}" class="btn btn-light ms-2">Cancel</a></div></form></div></div></div></div>
@endsection
@push('scripts')<script>function addCountLine(){const row=document.querySelector('.count-line').cloneNode(true);row.querySelectorAll('select,input').forEach(el=>{el.value='';el.removeAttribute('required');});document.getElementById('count-lines').appendChild(row);}</script>@endpush
