<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>{{ $count->recount_required ? 'Enter Recount' : 'Enter Physical Count' }}: {{ $count->count_no }}</h4><p class="text-muted">{{ $count->recount_required ? 'A different user must enter every recount quantity.' : 'System quantities are snapshots. Enter the verified physical quantities before submitting for approval.' }}</p></div><div class="card"><div class="card-body"><form method="POST" action="{{ route('inventory.counts.update', $count->id) }}">@csrf @method('PUT')<div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>System snapshot</th><th>Original count</th><th>{{ $count->recount_required ? 'Recount quantity' : 'Physical quantity' }}</th></tr></thead><tbody>@foreach($count->lines as $line)<tr><td>{{ $line->product->name }}</td><td>{{ number_format((float) $line->system_quantity, 6) }}</td><td>{{ number_format((float) $line->counted_quantity, 6) }}</td><td><input name="counted_quantity[{{ $line->product_id }}]" type="number" min="0" step="0.000001" value="{{ old('counted_quantity.'.$line->product_id, $count->recount_required ? $line->recounted_quantity : $line->counted_quantity) }}" class="form-control" required></td></tr>@endforeach</tbody></table></div><button class="btn btn-primary">{{ $count->recount_required ? 'Submit recount' : 'Submit for approval' }}</button><a href="{{ route('inventory.counts.index') }}" class="btn btn-light ms-2">Cancel</a></form></div></div></div></div>
