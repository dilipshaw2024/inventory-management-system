@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Batch Expiry &amp; Best-before Alerts</h4><p class="text-muted">Batches reaching expiry or best-before within the selected horizon, including already expired batches.</p></div><div class="card mb-3"><div class="card-body"><form method="GET" class="row g-2 align-items-end"><div class="col-md-3"><label class="form-label">Alert horizon (days)</label><input name="days" type="number" min="0" max="3650" value="{{ $days }}" class="form-control"></div><div class="col-md-3"><button class="btn btn-primary">Refresh</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Batch / lot</th><th>Manufactured</th><th>Best before</th><th>Expiry</th><th>Warranty</th><th>Serials</th><th>Status</th></tr></thead><tbody>@forelse($batches as $batch)<tr><td>{{ $batch->product->name }}</td><td>{{ $batch->batch_no }}{{ $batch->lot_no ? ' / '.$batch->lot_no : '' }}</td><td>{{ optional($batch->manufacturing_date)->format('Y-m-d') ?? '—' }}</td><td>{{ optional($batch->best_before_date)->format('Y-m-d') ?? '—' }}</td><td>{{ optional($batch->expiry_date)->format('Y-m-d') ?? '—' }}</td><td>{{ optional($batch->warranty_until)->format('Y-m-d') ?? '—' }}</td><td>{{ $batch->serials->count() }}</td><td>@if(($batch->expiry_date ?? $batch->best_before_date)?->isPast())<span class="badge bg-danger">Past date</span>@else<span class="badge bg-warning text-dark">Approaching</span>@endif</td></tr>@empty<tr><td colspan="8" class="text-center">No batches within this horizon.</td></tr>@endforelse</tbody></table></div>{{ $batches->links() }}</div></div></div></div>
@endsection
