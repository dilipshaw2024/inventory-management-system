@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="d-flex justify-content-between align-items-center mb-3"><div><h4>Stock Receipts & Issues</h4><p class="text-muted mb-0">Independent warehouse movements with approval and ledger posting.</p></div><a href="{{ route('inventory.documents.create') }}" class="btn btn-primary">New document</a></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>No.</th><th>Type</th><th>Date</th><th>Location</th><th>Status</th><th>Created by</th><th>Action</th></tr></thead><tbody>@forelse($documents as $document)<tr><td>{{ $document->document_no }}</td><td><span class="badge bg-{{ $document->document_type === 'receipt' ? 'success' : 'warning' }}">{{ ucfirst($document->document_type) }}</span></td><td>{{ optional($document->date)->format('Y-m-d') }}</td><td>{{ $document->location?->code ?? 'All locations' }}</td><td>{{ ucfirst($document->status) }}</td><td>{{ $document->creator?->name ?? 'System' }}</td><td>@if($document->status === 'pending')<form method="POST" action="{{ route('inventory.documents.approve', $document->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-success">Approve</button></form><form method="POST" action="{{ route('inventory.documents.reject', $document->id) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-danger">Reject</button></form>@else—@endif</td></tr>@empty<tr><td colspan="7" class="text-center text-muted">No independent stock documents found.</td></tr>@endforelse</tbody></table></div>{{ $documents->links() }}</div></div></div></div>
<div class="card mt-3"><div class="card-body"><h5>Pending receipt inspections</h5><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Document</th><th>Inspection</th><th>Notes</th><th>Control</th></tr></thead><tbody>@forelse($documents->filter(fn ($document) => $document->document_type === 'receipt' && $document->status === 'pending' && ($document->inspection_status ?? 'not_required') === 'pending') as $document)<tr><td>{{ $document->document_no }}</td><td><form method="POST" action="{{ route('inventory.documents.inspect', $document->id) }}" class="d-flex gap-1">@csrf<select name="inspection_status" class="form-select form-select-sm" required><option value="passed">Pass</option><option value="failed">Fail</option></select><input name="inspection_notes" class="form-control form-control-sm" placeholder="Inspection notes" required maxlength="3000"><button class="btn btn-sm btn-outline-primary">Record</button></form></td><td>{{ $document->inspection_notes ?: 'Awaiting inspection' }}</td><td>Approval remains blocked until passed.</td></tr>@empty<tr><td colspan="4" class="text-center">No stock receipts await inspection.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
