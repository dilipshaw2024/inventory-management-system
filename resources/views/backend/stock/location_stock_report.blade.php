@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Location Stock</h4><p class="text-muted">Ledger-derived physical balances by warehouse and storage location. Reservation quantities are excluded from physical stock.</p></div><div class="card mb-3"><div class="card-body"><form method="GET" action="{{ route('stock.location.stock') }}" class="row g-2 align-items-end"><div class="col-md-4"><label class="form-label">Product</label><select name="product_id" class="form-select"><option value="">All products</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(request('product_id') == $product->id)>{{ $product->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Location</label><select name="location_id" class="form-select"><option value="">All locations</option>@foreach($locations as $location)<option value="{{ $location->id }}" @selected(request('location_id') == $location->id)>{{ $location->warehouse->name }} / {{ $location->code }}</option>@endforeach</select></div><div class="col-md-4"><button class="btn btn-primary">Filter</button> <a href="{{ route('stock.location.stock') }}" class="btn btn-light">Reset</a></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Warehouse</th><th>Location</th><th>Type</th><th>Physical balance</th></tr></thead><tbody>@forelse($balances as $row)<tr><td>{{ $row->product->name }}</td><td>{{ data_get($row, 'location.warehouse.name', 'N/A') }}</td><td>{{ $row->location->code }}</td><td>{{ ucfirst($row->location->type) }}</td><td>{{ number_format((float) $row->balance, 6) }}</td></tr>@empty<tr><td colspan="5" class="text-center">No location balances found.</td></tr>@endforelse</tbody></table></div>{{ $balances->links() }}</div></div></div></div>
@endsection
