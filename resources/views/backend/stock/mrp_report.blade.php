@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>MRP Net Requirements</h4><p class="text-muted">Aggregated leaf-material demand from active multi-level BOMs, netted against current stock.</p></div>@if($errors)<div class="alert alert-warning">@foreach($errors as $error)<div>{{ $error }}</div>@endforeach</div>@endif<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Material</th><th>Supplier</th><th>Gross requirement</th><th>On hand</th><th>Shortage</th><th>Suggested price</th><th>Recommendation</th><th>Action</th></tr></thead><tbody>@forelse($proposals as $row)<tr><td>{{ $row['product']->name }}</td><td>{{ data_get($row['product'], 'supplier.name', 'N/A') }}</td><td>{{ number_format($row['required'], 3) }}</td><td>{{ number_format($row['on_hand'], 3) }}</td><td><strong class="text-danger">{{ number_format($row['shortage'], 3) }}</strong></td><td>{{ $row['price'] ? $row['price']->unit_price.' '.$row['price']->currency_code : ($row['product']->purchase_price ?? '—') }}</td><td><span class="badge bg-{{ $row['action'] === 'Buy' ? 'primary' : 'warning' }}">{{ $row['action'] }}</span></td><td>@if($row['action'] === 'Buy')<a class="btn btn-sm btn-primary" href="{{ route('procurement.orders.create', ['product_id' => $row['product']->id, 'supplier_id' => $row['product']->supplier_id, 'quantity' => $row['shortage'], 'price' => $row['price']?->unit_price ?? $row['product']->purchase_price]) }}">Create PO</a>@else—@endif</td></tr>@empty<tr><td colspan="8" class="text-center">No net material shortages detected.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
