@extends('admin.admin_master')
@section('admin')
<div class="card mb-3"><div class="card-body"><h5>Confirm put-away task</h5><p class="text-muted">This creates a normal approval-required transfer; stock is posted only after dispatch and receipt.</p><form method="POST" action="{{ route('warehouse.put.away.confirm') }}" class="row g-2 align-items-end">@csrf<div class="col-md-3"><label>Product</label><select name="product_id" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></div><div class="col-md-3"><label>Receiving/source location</label><select name="source_location_id" class="form-select" required><option value="">Select source</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->code }} — {{ $location->name }}</option>@endforeach</select></div><div class="col-md-3"><label>Destination bin/shelf/rack</label><select name="destination_location_id" class="form-select" required><option value="">Select destination</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->code }} — {{ $location->name }}</option>@endforeach</select></div><div class="col-md-2"><label>Quantity</label><input name="quantity" type="number" min="0.000001" step="0.000001" class="form-control" required></div><div class="col-md-1"><button class="btn btn-success">Create</button></div></form></div></div>
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Put-away Planner</h4><p class="text-muted">Capacity-aware location suggestions for incoming stock. Confirm the selected location on the receipt document.</p></div><div class="card mb-3"><div class="card-body"><form class="row g-2 align-items-end"><div class="col-md-5"><label>Product</label><select name="product_id" class="form-select"><option value="">Any product</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(request('product_id') == $product->id)>{{ $product->name }}</option>@endforeach</select></div><div class="col-md-3"><label>Quantity</label><input name="quantity" type="number" min="0.000001" step="0.000001" required value="{{ request('quantity', $quantity ?: '') }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary">Suggest</button></div></form></div></div><div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><thead><tr><th>Location</th><th>Warehouse</th><th>Occupied</th><th>Available capacity</th><th>Recommendation</th></tr></thead><tbody>@forelse($locations as $location)<tr><td>{{ $location->code }} — {{ $location->name }}</td><td>{{ $location->warehouse->name }}</td><td>{{ number_format($location->occupied_quantity, 3) }}</td><td>{{ $location->available_capacity === null ? 'Unlimited' : number_format($location->available_capacity, 3) }}</td><td>{{ $loop->first && $quantity > 0 ? 'Recommended' : 'Available' }}</td></tr>@empty<tr><td colspan="5" class="text-center">No location has capacity for this quantity.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
