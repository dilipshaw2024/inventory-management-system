@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box d-flex justify-content-between"><div><h4>Quarantine, Damaged & Scrap</h4><p class="text-muted">Status balances separate non-available stock from the legacy total product balance.</p></div><a href="{{ route('inventory.status.create') }}" class="btn btn-primary">New status transfer</a></div><div class="card mb-3"><div class="card-body"><h5>Status balances</h5><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Location</th><th>Status</th><th>Quantity</th></tr></thead><tbody>@forelse($balances as $balance)<tr><td>{{ $balance->product->name }}</td><td>{{ data_get($balance, 'location.code', 'Unassigned') }}</td><td>{{ ucfirst($balance->status) }}</td><td>{{ $balance->quantity }}</td></tr>@empty<tr><td colspan="4" class="text-center">No non-available stock.</td></tr>@endforelse</tbody></table></div>{{ $balances->links('pagination::bootstrap-4') }}</div></div><div class="card"><div class="card-body"><h5>Transfers</h5><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Number</th><th>Product</th><th>Move</th><th>Qty</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($transfers as $transfer)<tr><td>{{ $transfer->transfer_no }}</td><td>{{ $transfer->product->name }}</td><td>{{ ucfirst($transfer->from_status) }} → {{ ucfirst($transfer->to_status) }}</td><td>{{ $transfer->quantity }}</td><td>{{ ucfirst($transfer->status) }}</td><td>@if($transfer->status === 'pending')<form method="POST" action="{{ route('inventory.status.approve', $transfer->id) }}">@csrf<button class="btn btn-sm btn-outline-success">Approve</button></form>@else — @endif</td></tr>@empty<tr><td colspan="6" class="text-center">No transfers found.</td></tr>@endforelse</tbody></table></div>{{ $transfers->links() }}</div></div></div></div>
<div class="card mt-3"><div class="card-body"><h5>Approval rejection</h5><p class="text-muted">Reject pending quality-status transfers before balances change.</p><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Transfer</th><th>Action</th></tr></thead><tbody>@foreach($transfers as $pendingTransfer)@if($pendingTransfer->status === 'pending')<tr><td>{{ $pendingTransfer->transfer_no }}</td><td><form method="POST" action="{{ route('inventory.status.reject', $pendingTransfer->id) }}" class="d-inline">@csrf<input name="rejection_reason" value="Rejected by approver" class="form-control form-control-sm d-inline-block" style="width:160px" required maxlength="2000"><button class="btn btn-outline-warning btn-sm">Reject</button></form></td></tr>@endif @endforeach</tbody></table></div></div></div>
<div class="card mt-3"><div class="card-body"><h5>Pending quality inspections</h5><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Transfer</th><th>Action</th></tr></thead><tbody>@forelse($transfers->filter(fn ($transfer) => $transfer->status === 'pending' && $transfer->inspection_required && $transfer->inspection_status === 'pending') as $pendingTransfer)<tr><td>{{ $pendingTransfer->transfer_no }}</td><td><form method="POST" action="{{ route('inventory.status.inspect', $pendingTransfer->id) }}" class="d-flex gap-1">@csrf<select name="inspection_status" class="form-select form-select-sm" required><option value="passed">Pass</option><option value="failed">Fail</option></select><input name="inspection_notes" class="form-control form-control-sm" placeholder="Inspection notes" required maxlength="3000"><button class="btn btn-sm btn-outline-primary">Record</button></form></td></tr>@empty<tr><td colspan="2" class="text-center">No status transfers await inspection.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
