@extends('admin.admin_master')
@section('admin')
<div class="page-content"><div class="container-fluid"><div class="page-title-box"><h4>Inventory Valuation</h4><p class="text-muted">Value cost layers at the selected product, category, location, batch, and as-of date.</p></div><div class="card mb-3"><div class="card-body"><form method="GET" action="{{ route('stock.valuation') }}" class="row g-2 align-items-end"><div class="col-md-2"><label>Product</label><select name="product_id" class="form-select"><option value="">All products</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(request('product_id') == $product->id)>{{ $product->name }}</option>@endforeach</select></div><div class="col-md-2"><label>Category</label><select name="category_id" class="form-select"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id') == $category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-2"><label>Location</label><select name="location_id" class="form-select"><option value="">All locations</option>@foreach($locations as $location)<option value="{{ $location->id }}" @selected(request('location_id') == $location->id)>{{ $location->code }} — {{ $location->name }}</option>@endforeach</select></div><div class="col-md-3"><label>Batch/Lot</label><select name="batch_id" class="form-select"><option value="">All batches</option>@foreach($batches as $batch)<option value="{{ $batch->id }}" @selected(request('batch_id') == $batch->id)>{{ $batch->product->name }} — {{ $batch->batch_no ?: $batch->lot_no }}</option>@endforeach</select></div><div class="col-md-2"><label>As of</label><input name="as_of" type="date" value="{{ request('as_of') }}" class="form-control"></div><div class="col-md-1"><button class="btn btn-primary">Filter</button></div></form></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Product</th><th>Category</th><th>Quantity balance</th><th>Cost-layer value</th></tr></thead><tbody>@forelse($valuation as $product)<tr><td>{{ $product->name }}</td><td>{{ data_get($product, 'category.name', 'N/A') }}</td><td>{{ $product->filtered_quantity ?? $product->quantity }}</td><td>{{ number_format((float) $product->ledger_value, 2) }}</td></tr>@empty<tr><td colspan="4" class="text-center">No products found.</td></tr>@endforelse</tbody></table></div>{{ $valuation->links() }}</div></div></div></div>
@endsection
